# Decisions log

Minor mechanical choices made under BUILD-PROMPT §8.2 — smallest choice consistent
with existing patterns. One line each: date · choice · reason. Anything bigger goes
through the must-ask protocol (§8.3) or a Gymnasium proposal.

---

- 2026-08-26 · `.nvmrc` pins `20` (installed toolchain is Node 20.16.0; docs require 18+) · matches the machine CI and dev actually run on.
- 2026-08-26 · Toolchain sub-dependencies of the §10 pre-approved set installed without a memo: `vite` + `@vitejs/plugin-react` (required by electron-vite for React), `typescript-eslint` + `eslint-config-prettier` (required for ESLint to parse TS / not fight Prettier), `@types/node|react|react-dom` (type-only) · treated as "implied by the docs" (BUILD-PROMPT §7), flagged in the session report for Architect review.
- 2026-08-26 · Import-boundary lint implemented with core `no-restricted-imports` per-directory overrides instead of an extra plugin · zero new dependencies, same enforcement.
- 2026-08-26 · `postinstall` electron-rebuild deferred until `node-pty` lands in M0.3 · nothing native to rebuild before that.
- 2026-08-26 · Pixel fonts (Press Start 2P, Pixelify Sans, IBM Plex Mono) not yet bundled; token files declare the stacks with monospace fallback · font bundling owed by M0.4 floor/visual package.
- 2026-08-26 · `vite` pinned `^7` and `@vitejs/plugin-react` `^5` · electron-vite@5 peers on vite ≤7; npm's default pick (vite 8) is ERESOLVE-incompatible.
- 2026-08-26 · `@eslint/js` added as explicit devDependency · ESLint 10 no longer exposes it transitively; required by the flat-config recommended preset.
- 2026-08-26 · `electron` pinned to `^37` · electron ≥38 requires Node ≥22.12 at install time; this machine + `.nvmrc` + CI run Node 20. Revisit when Node is upgraded (flagged in session report).
- 2026-08-26 · Vitest config lives in `vitest.config.mts` (ESM) · vitest 4 cannot load a CJS-interpreted `.ts` config in a non-`"type": "module"` package.
- 2026-08-26 · `@xterm/addon-fit` installed alongside pre-approved `@xterm/xterm` · standard first-party companion for sizing the terminal to its panel; flagged for Architect review with the other toolchain implieds.
- 2026-08-26 · `scripts/patch-node-pty.cjs` runs in postinstall before electron-rebuild · two Windows build defects in node-pty 1.1.0: (a) winpty.gyp calls helper `.bat`s by bare name, which current Win11 builds no longer resolve from the cwd — patched to `.\`-prefixed; (b) its gyps demand Spectre-mitigated MSVC libs this machine's VS lacks (MSB8040) — patched to `SpectreMitigation: false`. Idempotent, Windows-only, no-ops elsewhere.
- 2026-08-26 · `src/shared/ipc.ts` must stay free of runtime deps (zod) because the sandboxed preload imports it and sandboxed preloads cannot require external modules · channel names/helpers live there; zod schemas live in the sibling schema modules imported only by main.
- 2026-08-26 · Vitest suites do not import `node-pty` (validators only) · node_modules build is Electron-ABI after electron-rebuild and cannot load under the Node test runner; PTY behavior is covered by the M1 conformance/E2E rigs per TEST-STRATEGY.
- 2026-08-26 · `pixi.js/unsafe-eval` imported in the floor module · Pixi's default shader codegen needs eval, which the strict CSP (ENGINEERING-STANDARDS §5) forbids; this official module swaps in eval-free paths — CSP stays strict.
- 2026-08-26 · `config:get` returns `{ config, warning }` (ConfigSnapshot) · an invalid `~/.ephesus/config.json` must surface visibly (BUILD-PROMPT §3.7); the file is never silently overwritten, the app runs on defaults with the warning shown in the status line.
- 2026-08-26 · `postinstall` now `electron-rebuild -f` (all native deps) · better-sqlite3 joined node-pty; rebuilding everything is simpler than maintaining a module whitelist.
- 2026-08-26 · `@types/better-sqlite3` added (type-only) · better-sqlite3 ships no types.
- 2026-08-26 · `EPH_HOME` env override for the harness home root · lets tests/E2E boot against a temp home (TEST-STRATEGY §4) without touching the real `~/.ephesus/`.
- 2026-08-26 · (M0 audit) `pty:ensure-dev-shell` is a temporary M0 channel · replaced by `agents.spawn` in M1; retire it when the claude adapter lands.
- 2026-08-26 · (M0 audit) `pty.kill` lives in the `pty:` group for M0 · SDD §5 homes `kill(id)` under `agents:`; folds into the agents group in M1 with the adapter lifecycle.
- 2026-08-26 · (M0 audit) `pty:exit:<id>` push event added beside `pty:data:<id>` · needed to make kill/exit outcomes visible; SDD §5 calls its event list abridged.
- 2026-08-26 · (M0 audit) Pixel-font bundling debt re-scoped: not delivered in M0.4 as originally logged · owed by the first M1 package that touches renderer chrome (M1.6 command bar) or earlier; awaiting Architect acknowledgment.
- 2026-08-26 · (Architect directive) MemPalace adopted as the Library's recall index and company archive · ADR-0016; docs updated across README, SDD §1.1/§2/§12, IMPLEMENTATION M4, BUILD-PROMPT §10; lands in M4, optional dependency with the FTS/grep degrade ladder intact.
- 2026-08-26 · (Architect directive) Floor art quality bar raised to Munder Difflin level · UI-DESIGN §7 rewritten (licensed tileset path + attribution rules + walk-cycle citizen bar); new package M1.5b in PROGRESS/BUILD-PROMPT; supersedes the "no licensed-asset dependency in v1" stance. Font debt folded into M1.5b (closes the pending acknowledgment above).
- 2026-08-26 · (M1.1) `EngineId`/`HookSupport` live in `src/shared/engines.ts`, the `EngineAdapter` interface in `src/main/engines/types.ts` · the renderer's agent card needs the id + fidelity grade (type-only import), but the adapter surface itself is main-only and must stay behind the boundary lint (NFR-12).
- 2026-08-26 · (M1.1) `SpawnPlan.settings` is a `SettingsInjection[]` fed by `wireHooks()`'s `HookPlan.injections` · ADR-0009 puts shim install in `wireHooks` while SDD §3 says `SpawnPlan` composes settings injection; making them one concept with one owner avoids two competing representations of the same files.
- 2026-08-26 · (M1.1) `AgentSpawnConfig` carries `hookEndpoint` (socket path / Windows pipe name) · the shim has to be told where to POST; the smallest honest representation, and it keeps M1.3's platform difference out of the adapters.
- 2026-08-26 · (M1.1) `ResumeSupport` is only `resumeArgs(sessionId)` · session-id *discovery* belongs to the event plane (hook payloads carry it, M1.3) — inventing a discovery method here would be adapter surface the ADR does not name.
- 2026-08-26 · (M1.1) `KeySequence` carries `label` beside `bytes` · anything the harness writes into an agent on the Architect's behalf must be inspectable (ENGINEERING-STANDARDS §4 "no hidden side effects for agents").
- 2026-08-26 · (M1.1) `HOOK_SUPPORT_RANK` encodes ADR-0009's `native > wrapper > pty-heuristic` numerically · M1.7's hook-grade-honesty check compares declared vs demonstrated fidelity; one shared ordering beats a switch in every caller.
- 2026-08-26 · (M1.1) Registry is an `EngineRegistry` class plus a process-wide `engines` instance (mirrors `PtyManager`) · tests build isolated registries instead of needing a test-only reset API, and a duplicate id throws rather than silently replacing an adapter (a replaced adapter would make the agent card's fidelity grade a lie).
- 2026-08-26 · (M1.2) The fake engine and the hook client are dependency-free ESM `.mjs` with `@ts-check`, not `.ts` · both run under whatever bare `node` spawns them, with no build step and no guaranteed `node_modules`; `tsconfig.node.json` gained `allowJs`/`checkJs` and `shims/**` so `npm run typecheck` still covers them (ENGINEERING-STANDARDS §1 type safety kept without a build).
- 2026-08-26 · (M1.2) The hook *envelope* schema landed here in `src/shared/hooks.ts`, ahead of M1.3's hook server · the fake emits a schema'd wire format the moment it exists, and invariant §9 forbids shipping one without a versioned validator; M1.3 adds the per-event payload schemas and the drift path on top of it.
- 2026-08-26 · (M1.2) Envelope `event` is a free string, with the known set in a separate enum plus `classifyHookEvent()` · FR-2.3 requires drift to be *accepted with a visible warning*, so an unknown event name has to survive parsing to be classified rather than being rejected at the schema.
- 2026-08-26 · (M1.2) Hook event names are harness-normalized (`pre-tool`), not engine-native (`PreToolUse`) · mapping engine vocabulary is the adapter's job; otherwise `hooks.ts`, the avatar machine and the floor would learn a Claude-ism and NFR-12 would be violated at the first second engine.
- 2026-08-26 · (M1.2) `HOOK_EVENTS` adds `session-start`/`session-end` to the SDD §6 socket-borne triggers · they bind a spawn to an engine session id, which is what resume (FR-1.4) and `TranscriptReader` (FR-11.2) key off. `notification` (the engine's native permission-prompt choke point, SDD §9) is deliberately left out until M3 wires it — the drift path reports it honestly meanwhile.
- 2026-08-26 · (M1.2) The fake's script format is validated by hand inside the fake, not by a zod schema in `src/shared/` · it is a test-double format; production shared code should not carry test types. It still carries `schemaVersion` and refuses a drifted version loudly (exit 2), which is what invariant §9 is actually protecting.
- 2026-08-26 · (M1.2) The fake parses stdin character by character — Enter submits the pending line, Escape clears it and interrupts · a PTY can hand over a typed prompt and the interrupt key in a single read, and per-chunk parsing silently swallowed one of them (caught by the M1.2 smoke test before it could mislead M1.6).
- 2026-08-26 · (M1.2) The fake supports `onPrompt`/`onInterrupt` reactions beyond M1.2's brief · M1.7 conformance owes a spawn/interrupt/kill lifecycle green for the *fake* adapter, so the double has to answer an interrupt; building it now avoids re-opening the fake in M1.7.
- 2026-08-26 · (M1.2) `read-inbox` takes `consume: true`, moving messages to `inbox/.done/` · `.done/` is SDD §2's own directory and M2's S-WAKE cursor-idempotency scripts need the fake to consume; the default stays non-destructive.
- 2026-08-26 · (M1.3) The Windows pipe name is `\.\pipe\ephesus-events-<sha256(resolved home)[0:16]>`, not `<uid>` as the M1 plan sketched · Windows has no uid, and pipe names live in a machine-wide namespace, so the discriminator must separate harness homes rather than users — which is also what lets every `EPH_HOME` test bind its own endpoint in parallel. POSIX is unchanged: `<home>/events.sock`, chmod 0600 (SDD §2).
- 2026-08-26 · (M1.3) On Windows the documented 0600 requirement is met by the local-namespace pipe (libuv rejects remote clients) plus the per-payload token, since a pipe carries no file mode · recorded because ENGINEERING-STANDARDS §5 states the rule in POSIX terms and this is the equivalent, asserted by its own platform-gated test.
- 2026-08-26 · (M1.3) Per-event payload schemas are *loose* (`z.looseObject`), and only `pre-tool`/`post-tool` require a field (`tool`) · an engine adding a payload field has broken nothing; the station map (SDD §6) genuinely cannot route an avatar without a tool name, so that absence is the one payload drift worth a warning.
- 2026-08-26 · (M1.3) Rejection reasons never contain the presented or expected token · they are credentials and BUILD-PROMPT §3.6 keeps secret-shaped strings out of logs; the reason names the agent instead, which is the ref a reader actually needs.
- 2026-08-26 · (M1.3) The hook server takes `onEvent`/`onRejected` callbacks rather than writing anywhere itself · `log.jsonl` and the single committer arrive in M2, and this keeps the endpoint's sink swappable without touching the transport. Main wires both to console with refs meanwhile.
- 2026-08-26 · (M1.3) Distinct drift warnings are retained in-server (`driftWarnings()`), deduped and in first-seen order · FR-2.3 needs them surfaced in the UI, and M1.5 is the first package where hook events reach the renderer; the obligation is recorded against M1.5 in PROGRESS so it cannot be lost.
- 2026-08-26 · (M1.3) The endpoint binds in `app.whenReady()` before the window is created · a spawn must never race its own hooks, and there is no ordering subtlety to remember later if the socket is up before anything can spawn.
- 2026-08-26 · (M1.4) `eph-hook.mjs` is engine-agnostic; the engine→harness event mapping and payload key renames arrive as arguments the adapter writes into the settings file (`--event pre-tool --field tool=tool_name --session-field session_id`) · one shim for every engine, and Claude-isms stay inside `engines/claude.ts` where NFR-12 wants them. Renamed keys are added, never substituted, so nothing the engine sent is lost.
- 2026-08-26 · (M1.4) Claude Code's hook set maps 1:1 onto `HOOK_EVENTS`, `PostCompact` included · verified against the engine's documented hook list; no harness event is synthesised and none is dropped, which is what makes the declared `native` grade honest.
- 2026-08-26 · (M1.4) The shim writes nothing to stdout, only stderr · engines treat hook stdout as instructions to the model; a chatty shim would inject text into the agent's context.
- 2026-08-26 · (M1.4) Agent base env is an allowlist (`src/main/engines/spawn-env.ts`), not `process.env` · ADR-0010 gives agents credentials only through declared grants, and inheriting the harness's whole environment would defeat that on the first spawn. **Raised in the session report for Architect ratification** — the policy follows ADR-0010, but the exact list is a judgment call.
- 2026-08-26 · (M1.4) `injectIdentity()` for claude validates and renders rather than writing anything · the engine takes identity on the command line (`--append-system-prompt`, an ADR-0009-sanctioned mechanism), so `spawnArgs` performs the injection; what `injectIdentity` owes is the precondition — missing `identity.md`/`PROTOCOL.md` throws at spawn instead of an agent quietly running with no idea who it is.
- 2026-08-26 · (M1.4) The identity appendix template lives at `prompts/engines/identity-appendix.md` and is read through `PromptStore` (home copy first, bundled copy as seed) · invariant §8 — prompt text is config, and it only counts if the copy the Architect can edit is the copy actually read.
- 2026-08-26 · (M1.4) `settings.local.json` is *merged*, not replaced, and the prior file is backed up byte-for-byte to `settings.local.json.eph-backup` · the Architect's own local settings and hooks keep working while an agent runs, and uninstall restores exactly what was there (ADR-0009 hygiene; the byte-for-byte requirement is why `writeFileAtomic` now accepts a Buffer).
- 2026-08-26 · (M1.4) An existing `settings.local.json` that is not valid JSON makes `wireHooks()` throw instead of overwriting it · backing up something unparseable and writing our own over it would look like it worked; refusing to spawn until the file is fixed is the honest answer.
- 2026-08-26 · (M1.4) Packaging debt recorded: `prompts/` and `shims/` are resolved from the app path, so a packaged build must ship them as extra resources (a shim inside an asar cannot be executed by `node`) · owed by M7 packaging, harmless in dev and CI.
- 2026-08-26 · (M1.4b) `AgentManager` takes an `AgentSpawner` seam instead of importing `PtyManager` · node-pty is Electron-ABI after `electron-rebuild` and cannot load under vitest, so injecting the spawner is what makes the lifecycle rules (ordering, rollback, token lifetime) testable per-PR rather than E2E-only.
- 2026-08-26 · (M1.4b) `argv[0]` is resolved against the *plan's* PATH before `pty.spawn` (`src/main/which.ts`) · a conpty spawn on Windows does not walk PATH/PATHEXT, so a bare `claude` (an npm `.cmd`/`.exe` shim) died with ERROR_FILE_NOT_FOUND. Found by running the real app, not by reading code — the adapter keeps naming its binary logically and the platform detail stays in one place.
- 2026-08-26 · (M1.4b) The agent id is claimed synchronously before `spawn()`'s first await · two spawn calls in flight for one agent both passed the liveness check, and the second's hook token replaced the first's in the endpoint registry, leaving the running agent's hooks rejected with a token mismatch. Observed live; regression test named for it.
- 2026-08-26 · (M1.4b) `starting` added to `AGENT_LIFECYCLES` · the reservation window (probe + settings install) is real elapsed time during which the id is claimed and no process exists; showing `running` through it would be a lie on the agent card.
- 2026-08-26 · (M1.4b) A `spawn` may replace an agent whose lifecycle is `exited` or `missing-binary`, and is refused otherwise · that is the FR-1.4 respawn offer; refusing every duplicate would make a crashed agent unrecoverable without a restart.
- 2026-08-26 · (M1.4b) `agents.send(id, text)` writes Architect text into the agent's PTY verbatim · FR-1.3's queue-until-idle decision belongs in main and lands with the command bar (M1.6); this is the transport it will use.
- 2026-08-26 · (M1.4b) The renderer receives agent-card changes on `state:agents` (SDD §5's push list) carrying one card, not the whole roster · card updates are frequent and per-agent; the renderer keeps a projection keyed by id.
- 2026-08-26 · (M1.5) Tool *classification* is the adapter's table, applied generically by the shim (`--classify`/`--class`/`--class-prefix`) · SDD §6's station map is keyed by tool class, and turning `Read`/`Bash`/`mcp__*` into a class is engine knowledge — a table of Claude tool names inside the floor would be exactly the NFR-12 leak the ADR forbids. An unmatched tool gets no class and the avatar works at its desk.
- 2026-08-26 · (M1.5) `arrive` is emitted by MAIN from the shared floor geometry, not by the renderer · SDD §6 has the edge but names no owner; giving it to the renderer would make the floor authoritative about when an agent reached a station, which ADR-0002 does not allow. `src/shared/floor.ts` holds the station tiles so both planes compute the same walk duration.
- 2026-08-26 · (M1.5) `AvatarSnapshot` carries `origin` beside `station` · a walk is only renderable as (from, to, since); without the origin the renderer would have to remember where the avatar was, i.e. hold state the model does not have.
- 2026-08-26 · (M1.5) `arrive` at the desk leaves the avatar `thinking`, not `working` · SDD §6's arrow reads `thinking ──arrive──► working`, but §2.4 defines `working` as "at a station, tool in use", and a desk is not a station a tool runs at. Resolving the ambiguity toward §2.4's definition; flagged in the session report.
- 2026-08-26 · (M1.5) `stopped` and `archived` are modelled as terminal phases beside the ten §6 states · both words appear in the §6 diagram as endpoints but not in its state list; they are not poses (the avatar has left the floor) and terminal phases absorb every later event.
- 2026-08-26 · (M1.5) `stop.pending` is always false until Hermes lands (M2) · the ADR-0013 autonomy branch asks "is mail or a task waiting", and today nothing can be waiting. That is the truth about this system, not a placeholder.
- 2026-08-26 · (M1.5) The renderer remembers each sprite's last drawn pixel position and starts an interrupted walk from there · a real tool usually finishes before its walk does, so the next snapshot describes a walk from a station the avatar never reached; drawing that literally teleports the sprite forward and then walks it back. Presentation smoothing only — phase and destination still come entirely from main. Found by watching the live run.
- 2026-08-26 · (M1.5) **Architect text must be written to a PTY as two writes: the text, then the submit key.** Claude Code's TUI turns on bracketed-paste mode (`ESC[?2004h`) once booted, and a single write ending in CR is taken as a paste rather than a submitted line — the agent silently held the text and never ran. Diagnosed live (early sends worked, later ones did not); binding on the M1.6 command bar.
- 2026-08-26 · (M1.5b) Citizens are a pure sprite-recipe module (`floor/citizen.ts`) returning rectangles, not canvas calls · UI-DESIGN §7's bar (8 directions, 4+ frames each, ≤5 palette colours, distinct silhouettes) is then a unit test rather than an opinion, which is also what TEST-STRATEGY §4 asks for — assert the scene's state model, not pixels.
- 2026-08-26 · (M1.5b) Eight directions are authored as five views (front, back, side, front-side, back-side) with the four westward ones mirrored · standard pixel-art construction; each view stays hand-shaped instead of interpolated, and the diagonals are distinguished by a narrower three-quarter torso and a visible ear on the back-side view.
- 2026-08-26 · (M1.5b) Silhouettes are assigned by a stable hash of the role string, with `orchestrator` reserved · roles are free-form in the registry (SDD §4.1), so a fixed role→silhouette table would be inventing a role vocabulary the docs do not have. Same role always looks the same.
- 2026-08-26 · (M1.5b) The status badge is drawn outside the sprite's five-colour budget · it is a UI marker (UI-DESIGN §8 double-encoding), not part of the citizen; counting it would force a real colour out of the recipe.
- 2026-08-26 · (M1.5b) The licensed tileset is *discovered*, never imported: `import.meta.glob` over a gitignored drop directory · a static import of a file that may not exist (or may not be redistributable) would break the build and the licence at once. Empty drop ⇒ procedural tiles plus a visible `tileset: procedural (none installed)` note.
- 2026-08-26 · (M1.5b) Pixel fonts are fetched and byte-checked at runtime rather than imported · a dev server answers a missing file with index.html, and handing that to the font parser produces a confusing "invalid sfntVersion" instead of the plain fact that the face is not installed. Missing faces are shown in the status strip (invariant §7).
- 2026-08-26 · (M1.6) The queue-until-idle decision is a pure function in `src/shared/commands.ts`, *made* in `src/main/commands.ts`, and only *rendered* by the command bar · FR-1.3's rule is one decision in one place; a text box that decided for itself would be holding authoritative state (ENGINEERING-STANDARDS §4), and the M1.6 risk note names exactly that.
- 2026-08-26 · (M1.6) The queue flushes off the same avatar snapshots the floor draws · "idle" then means one thing to the Architect's eye and to the queue, instead of two clocks that can disagree.
- 2026-08-26 · (M1.6) `success` counts as ready, not busy · it is a 250 ms flash on the way to `idle` (SDD §6) and the agent is already free; holding through it would add a visible stutter for nothing.
- 2026-08-26 · (M1.6) Held text accumulates (newline-joined) rather than replacing · an Architect who types twice while an agent works meant to say both things; dropping the first would be silent data loss.
- 2026-08-26 · (M1.6) Every held phase carries its own reason ("agent is mid-tool", "blocked at a gate", …) rather than a generic "busy" · the bar shows the reason, and "busy" tells the Architect nothing they could act on. Asserted by a test that rejects the generic string.
- 2026-08-26 · (M1.6) Interrupt clears the queue · stopping an agent did not mean "and then say this anyway" (FR-1.3). Wired in the `agents:interrupt` handler so it holds however the interrupt is triggered.
- 2026-08-26 · (M1.6) `SUBMIT_KEY_DELAY_MS = 150` between the text write and the CR · the two-write requirement came from a live diagnosis (bracketed paste swallows a trailing CR); 150 ms is above input-coalescing windows and imperceptible, and it is injectable so tests stay deterministic.
- 2026-08-26 · (M1.7) The fake engine's adapter takes engine id `custom` · ADR-0009 reserves that id for exactly this, so the roster needs no change to give the conformance suite a second implementation. A suite with one implementation only proves that implementation compiles.
- 2026-08-26 · (M1.7) The fake adapter deliberately uses a *different* mechanism for every part it can — identity through the environment rather than argv, its own settings filename, its own transcript format · the suite must test effects, not mechanisms (SDD §3), and only a second, differently-built adapter proves it does.
- 2026-08-26 · (M1.7) "Hook grade honesty" is operationalised as: `native` requires every `HOOK_EVENTS` name demonstrated, `wrapper` at least one, `pty-heuristic` none; an adapter is honest when demonstrated ≥ declared · ADR-0009 names the grades but not a measurement, and a check without one would be vacuous. The suite includes a *negative* case — an adapter claiming `native` while reporting two of eight events is caught — so the check is proven to bite.
- 2026-08-26 · (M1.7) The per-PR grade check for `claude` is the wiring, not a live spawn; the demonstrated-events check runs against the fake · TEST-STRATEGY §1.3 puts real-engine tests in the nightly suite, not per-PR. The claude adapter's live demonstration is recorded from this session's M1.4 run instead.
- 2026-08-26 · (M1.7) `claude` declares no `TranscriptReader` yet, and the suite records that as a declared capability gap rather than a failure · ADR-0009 marks `transcripts?` optional and says capability differences become visible product tiers; the reader needs real transcript fixtures and belongs with the cost ledger in M3.
- 2026-08-26 · (M1 exit) `pty:ensure-dev-shell` and `pty:kill` retired, closing the two M0-audit deferrals · the claude adapter has landed, so the terminal panel attaches to the *selected agent's* PTY (UC-03 step 2) and killing goes through `agents:kill` where SDD §5 homes it. The M0 dev shell is gone with them.
- 2026-08-26 · (M1 exit) `ptyIdSchema` widened to allow dots · a PTY id is now its agent's id (`AgentCard.ptyId`), and the old dash-only pattern rejected every `resize` for `agent.mason`. Caught by the exit review, not by a test — added to the pty validator suite.
- 2026-08-26 · (M1 exit) Typing in the terminal panel bypasses the command queue, by design · FR-1.3 governs *prompts*; answering the engine's own permission dialog or cycling its modes is the Architect operating the engine's interface, and holding those keystrokes until idle would deadlock (the agent is mid-tool *because* it is asking).
- 2026-08-26 · (M1 exit) A newly spawned agent takes the selection only when nothing is selected · an agent appearing must not yank the Architect's attention off the one they are watching.
- 2026-08-26 · (M1 exit) SDD §1.1 and §5 updated to name the M1 modules (`agents.ts`, `avatars.ts`, `commands.ts`, `prompts.ts`) and the `avatars`/`commands`/`hooks` IPC groups · IMPLEMENTATION §0.4: "a milestone isn't done if the SDD lies about the code". `fsx.ts`/`home.ts`/`index.ts` stay unlisted — the map is a subsystem map, not a file listing.
- 2026-08-26 · (M1 audit fix) FR-1.6 install offer now spawns with `baseAgentEnv()` instead of `{}` · an empty environment cannot resolve `npm` from PATH nor start it (the exact Windows conpty failure class DECISIONS-LOG M1.4b records); no EPH_* vars — the installer is not an agent yet. Env assertion added to the FR-1.6 test.
- 2026-08-26 · (M1 audit fix) `gate-verdict` restores the prior STATION, not just the phase · SDD §6 says "verdict → prior state"; the avatar now walks back from the watch post to where it was (`resumeStation` on the snapshot), with a named regression test. Previously it teleported to the desk in a phase/station combination no other path can produce.
- 2026-08-26 · (M1 audit fix) `sk-`-prefixed fixture value renamed to a scanner-neutral string · BUILD-PROMPT §3.6 has no obviously-fake exception, and `sk-` is what secret scanners key on.
- 2026-08-26 · (M1 audit) Floor status badge is color-only for `idle`↔`waiting` and `alert`↔`thinking` pairs · UI-DESIGN §8 deviation, low urgency — `waiting`/gates/breaker cannot fire before M2/M3; owed a glyph/label second encoding when M2.6's Activity tab or the §5 hover card lands.
- 2026-08-26 · (M1 audit) Claude adapter omits optional `resume` although Claude Code supports `--resume` · capability gap recorded like the M1.7 transcripts note; wire it with FR-1.4's respawn-offer work (M2's ghost path or M4's respawn-with-memory), not as adapter gold-plating now.
- 2026-08-26 · (Architect, M1 close) Pixel fonts sourced from npm: `@fontsource/press-start-2p`, `@fontsource/pixelify-sans`, `@fontsource/ibm-plex-mono` (3 new deps, Architect-approved) · `scripts/sync-fonts.cjs` copies the latin-400 woff2 files into the gitignored drop on every install; fonts.ts and its visible-missing state unchanged. Closes M1.5b's font must-ask.
- 2026-08-26 · (Architect, M1 close) M1 demo captured as screenshots in `docs/demo/` and embedded in README + the M1 implementation record · capture used a temporary env-gated auto-spawn in main, removed in the same commit; the README shot intentionally shows the interim (pre-tileset) art state.
- 2026-08-26 · (Architect verdict) All 15 toolchain sub-dependencies RATIFIED after list review: vite@7, @vitejs/plugin-react@5, typescript-eslint, @eslint/js, eslint-config-prettier, @electron/rebuild, @xterm/addon-fit, @types/{node,react,react-dom,better-sqlite3}, @fontsource/{press-start-2p,pixelify-sans,ibm-plex-mono} · closes the open item flagged since M0.1.
- 2026-08-26 · (Architect directive) Interim floor tileset: Kenney CC0 packs (Roguelike/RPG + Roguelike Indoors, licenses verified in-pack) staged in the gitignored tileset drop — app detects `tileset: 2 sheet(s)` · LimeZu Modern Interiors (whose free tier is private-use-only and repo-incompatible) will be purchased by the Architect and replace them with zero code change; rendering *from* sheets (beyond intake/detection) is owed by the art integration in M2+.
- 2026-08-26 · (M2.1) Every `git` invocation lives in `src/main/git.ts`, and `scripts/check-invariants.cjs` fails CI on a `git` call anywhere else · ADR-0004's single-committer claim is only worth what enforces it; the tripwire was proven to bite by planting a probe file (ENGINEERING-STANDARDS §5 asks for grep-able tripwires in CI). The same script flags truncating writes to `log.jsonl`/the cost ledger (invariant §5).
- 2026-08-26 · (M2.1) Commit identity is passed per invocation (`-c user.name=Ephesus …`), never written into the repo config, along with `commit.gpgsign=false` and a neutered `core.hooksPath` · the Agora must not inherit — or depend on — whatever the machine's global git config happens to be; CI runners frequently have no identity at all, and a user's global hooks must never run against the company's repo.
- 2026-08-26 · (M2.1) `ensureRepo()` commits ONLY when it actually seeded something · committing unconditionally swept up whatever a crashed run had left behind and labelled it "seed the Agora" — the work survived but the history misnamed why it landed, and ADR-0004 leans on that history for forensics. Caught in the live blackout run, not by a test; regression test added.
- 2026-08-26 · (M2.1) Fault points (`before-stage`/`after-stage`/`before-commit`/`after-commit`/`before-reconcile`) are in the production commit path, not a mock · S-BLACKOUT has to interrupt *between* staging and committing, and a seam that only exists under a mock proves nothing about the real ordering.
- 2026-08-26 · (M2.1) Retries live in the committer queue alone, with backoff-then-clear-lock ordering · BUILD-PROMPT §7 forbids retry loops around git elsewhere; a lock can only be stale here because this module is the only git caller and it serialises.
- 2026-08-26 · (M2.1) Settings install/uninstall moved out of each adapter into one `InstalledSettingsPlan` (`src/main/engines/settings-install.ts`) · claude.ts and the fake adapter carried byte-identical copies of the rule most likely to damage someone's repository, and a fix in one would not have reached the other. The conformance suite still passes for both adapters unchanged, which is what made the refactor safe.
- 2026-08-26 · (M2.1) The installation is recorded in the durable registry BEFORE the file is written · a crash between write and record would leave an untracked file in the Architect's tree, which is exactly the failure this closes.
- 2026-08-26 · (M2.1) `git init` is in `agora.ts`, not `home.ts` · keeps `home.ts` git-free and leaves `src/main/git.ts` as the only module the tripwire has to allowlist.
- 2026-08-26 · (M2.2) The log envelope (`ts`,`seq`,`kind`) is validated; the refs beside it are open (`z.looseObject`) · SDD §4.3 lists twenty kinds arriving across six milestones, each with different refs. A closed schema would mean editing `log.ts` every time a subsystem lands, and rejecting an event because a later milestone added a field would be losing the book of record to protect its formatting.
- 2026-08-26 · (M2.2) `ts` and `seq` are stamped by the appender, never accepted from the caller · a caller-supplied seq could collide and a caller-supplied timestamp could reorder history; NFR-13 depends on the log being reconstructible in order.
- 2026-08-26 · (M2.2) A torn final line is ignored on read and **left on disk**; the next append starts on a fresh line · truncating it would be a rewrite, and `log.jsonl` is append-only forever (invariant §5). Sequence numbering resumes from the last *intact* entry.
- 2026-08-26 · (M2.2) Registry fields are split required / required-nullable / optional following the two worked examples in SDD §4.1 (Artemis carries `status`/`hookFidelity`/`spawnedAt`, Mason carries `hire`) · rather than inventing one uniform shape neither example matches.
- 2026-08-26 · (M2.2) `canCloseTask()` returns *every* reason a task cannot close, not the first · a caller that fixes one obligation should not have to ask again to discover the next. Only `→ done` is guarded (SDD §4.2); inventing a fuller state machine would be improvisation BUILD-PROMPT §7 forbids.
- 2026-08-26 · (M2.2) A corrupt `registry.json`/`tasks.json` yields the empty default plus a retained warning, and the file is never rewritten · same degradation contract as M0's `config.json`: visible, non-fatal, non-destructive.
- 2026-08-26 · (M2.2) Lifecycle events reach the log through an injected `onLogEvent` on `AgentManager`, not an imported Agora · keeps the lifecycle testable without an Agora on disk and routes every spawn/exit through one place.
- 2026-08-27 · (M2.3) `requires_reply` is DERIVED from the act at compose time and re-checked at pickup; a message whose flag disagrees with its act is rejected · ADR-0003 calls the anti-livelock rules "transport rules, not etiquette", and a sender that could clear the flag could opt out of the obligation table.
- 2026-08-27 · (M2.3) A message in agent A's outbox claiming `from: agent.b` is rejected as a forgery · single-writer-per-file means an outbox carries only its owner's mail; without this check one agent could impersonate another by writing in its own directory.
- 2026-08-27 · (M2.3) A message the router cannot carry is parked in `outbox/.rejected/`, not deleted and not left in place · deleting loses what the agent got wrong, leaving it re-rejects forever. SDD §2 does not name this directory — smallest choice consistent with `inbox/.done/`.
- 2026-08-27 · (M2.3) Delivery ordering is rename → log → drain outbox, with no transaction · a crash before the rename loses nothing (the outbox file is still there); a crash after it delivers the same `<id>.json` twice at worst, and a second rename to the same path is a no-op. Both directions asserted with the fault seam.
- 2026-08-27 · (M2.3) `.done/` is the authority on what was consumed; `cursor.json` is the high-water mark · they answer different questions, and only `.done/` survives out-of-order delivery. Replay of an already-consumed id is dropped by the `.done/` check, not the cursor.
- 2026-08-27 · (M2.3) `sweep()` serialises on a promise chain like the committer does · the watcher and the periodic timer both fire on the same mail, and two passes racing over the same files would double-deliver.
- 2026-08-27 · (M2.3) Hermes exposes a `route()` seam that M2.4 fills with the hop-cap/bounce/broadcast rules · keeps delivery mechanics and routing policy separable, so S-LIVELOCK and S-BOUNCE can assert the rules at the module boundary.
- 2026-08-27 · (M2.3) Hermes fault points (`before-deliver`/`after-deliver`/`before-drain-outbox`/`before-consume`/`after-consume`) live in the production path · same reason as the Agora's: S-BLACKOUT must interrupt real ordering, not a mock's.
- 2026-08-27 · (M2 CI fix) The conformance suite's live-spawn helper sends the interrupt key exactly once, guards `stdin.writable`, and swallows a late `stdin` EPIPE · a stdout chunk arriving after the child was killed wrote to a dead pipe, and the async EPIPE failed the whole vitest run rather than an assertion. Green on Windows, red on CI's Linux — found only because CI was checked before the next package started.
- 2026-08-27 · (M2.4) `DEFAULT_HOP_CAP = 8` · ADR-0003 names the rule but not the number. Eight leaves room for a genuine multi-turn exchange (ask → clarify → answer → confirm, twice) while catching a two-agent ping-pong within seconds rather than minutes of budget. Configurable per `RoutingContext`.
- 2026-08-27 · (M2.4) The hop cap is checked BEFORE the address · a livelocked ping-pong must be stopped whether or not its recipient still exists; the point of the cap is to stop the loop, not to adjudicate the address. Asserted by a case that diverts a capped message aimed at a dead agent.
- 2026-08-27 · (M2.4) With no orchestrator yet, `to: "human"` and hop-cap diversions queue at `agora/human/` — outside `agents/` · FR-3.7 routes `human` to Artemis as the Architect's proxy, and until that proxy exists losing the message would be the worst outcome available. `human` is deliberately not an agent id and never appears in the roster.
- 2026-08-27 · (M2.4) A bounce is written straight into the sender's inbox, not through the sender's outbox · the sender did not write the refusal, and an outbox carries only its owner's mail (the same rule the forgery check enforces).
- 2026-08-27 · (M2.4) A broadcast with no recipient other than the sender bounces rather than delivering to nobody · "never drop silently" (FR-3.4) applies to a broadcast into an empty company too.
- 2026-08-27 · (M2.4) `needs_human` is carried and logged but does NOT change the address · SDD §4.4 calls it a flag the "router/Artemis may flip", and FR-3.7 makes `to:"human"` the addressing mechanism. Acting on the flag is Artemis's job in M3; re-routing on it here would be inventing a rule the docs do not have.
- 2026-08-27 · (M2.5) The three ADR-0013 guards are decided by ONE pure function (`decideStop`) so their ordering is visible and testable · `stop_hook_active` is checked first so a hook can never chain off its own continuation, and the cap is checked *before* pending work so a pathological loop stops even while mail keeps arriving. Reversing either would make the guards decorative.
- 2026-08-27 · (M2.5) `DEFAULT_BLOCK_CAP = 20`, `PATHOLOGY_SIGNAL_AT = 10` · ADR-0013 requires a cap to exist, not a particular value. The signal sits deliberately *below* the cap so the breaker (ADR-0011, M3) can steer at rung 1 before the backstop stops the work outright.
- 2026-08-27 · (M2.5) `decideStop` returns FACTS (`pendingMail`, `pendingTasks`), never prose · ADR-0013 calls the block reason "a real prompt surface subject to the same review as system prompts", so it is rendered by the caller from `prompts/hermes/stop-block-reason.md` (invariant §8). Asserted by a test that fails if the decision object contains English.
- 2026-08-27 · (M2.5) The Stop-hook reply travels back as the HTTP response body, and the shim relays it to stdout — but ONLY when it contains a decision · M1.4 made the shim silent on stdout because engines read it as instructions; the autonomy loop is precisely the case where that channel is the point. Silent-unless-instructed is preserved, with a regression test for both halves.
- 2026-08-27 · (M2.5) The wake watchdog nudges once per *pending episode*, keyed on the inbox emptying rather than on a timer · "exactly once" (FR-3.5) means once per batch of mail, not once ever; a nudge is suppressed entirely when the agent is not idle, which is the stale-nudge case ADR-0013 names.
- 2026-08-27 · (M2.5) The nudge is delivered through the command queue, not a raw PTY write · it is a prompt, so FR-1.3 queue-until-idle applies to it exactly as it does to the Architect own typing.
- 2026-08-27 · (M2.5) `stop.pending` is now the same fact Hermes gives the Stop hook, injected into `AvatarDirector` as `hasPendingWork` · the floor and the autonomy loop can no longer disagree about whether an agent is done. **Closes the second M1 carried item.**
- 2026-08-27 · (M2.5) Every stop decision is logged with its machine-readable `because` tag · a loop that continues silently is exactly the pathology R2 warns about, and this is where the breaker and the next briefing will read it.
- 2026-08-27 · (M2.6) The roster row is written from `AgentManager` through an injected `onRosterChange`, mirroring the same lifecycle the card and the log already mirror · one owner for the write, and the lifecycle stays testable without an Agora on disk.
- 2026-08-27 · (M2.6) Every hire gets `seat: 'terrace'` for now · SDD §4.1 shows `temple` for Artemis and `terrace-3` for a worker, but seat assignment belongs with the floor layout and Artemis's reserved seat in M3. A single honest placeholder beats inventing a numbering scheme the docs do not define.
- 2026-08-27 · (M2.6) The Activity tab holds a cursor and pages forward; `log:append` carries no payload, only "the log grew" · the panel is a pointer to `log.jsonl`, never a second record (NFR-13), and a push carrying rows would immediately become a second copy that could disagree with the file.
- 2026-08-27 · (M2.6) Appends are coalesced into one pull per 120 ms · SDD §11 asks for batched log rendering; a burst of thirty events costs one render rather than thirty.
- 2026-08-27 · (M2.6) The tab strip shows only Floor and Activity · UI-DESIGN §4 lists nine Command Center tabs, but offering a tab for a subsystem that does not exist would be inventing UI (BUILD-PROMPT §7). The rest arrive with their milestones.
- 2026-08-27 · (M2.7) The scenario suites run REAL spawned `fake-engine` processes over real git, a real socket and real files · every one of them is about what happens to state on disk when something goes wrong, and a mock cannot be interrupted between a rename and a commit. "Restart" is modelled by abandoning the objects mid-flight and building a fresh company over the same home, as a killed process and a new one would.
- 2026-08-27 · (M2.7) `clearStaleLock` retries and then gives up quietly instead of throwing · a git child of the process we just replaced can hold `index.lock` open for a moment on Windows, and failing to clear a lock is a delay the commit retry absorbs, not a boot worth losing. Found by S-BLACKOUT.
- 2026-08-27 · (M2.7) Temp-home cleanup is separated from `Company.close()` · a blackout scenario deliberately runs two companies over one home, and deleting the directory while the survivor still has a commit in flight is a teardown race, not a finding.
- 2026-08-27 · (M2.7, Architect-visible) The claude adapter now grants each agent file access to its OWN `agora/agents/<id>/` directory via `settings.local.json` permissions · an agent's mailbox lives in the harness home, outside its working directory, so the engine's permission model blocked every outbox write — FR-3.2 was unimplementable without it. Found by a real agent in the exit demo answering "the write was blocked by permissions". The grant is the narrowest thing that makes the documented design work: its own directory, never the Agora, never `agents/`, never a sibling mailbox. **Raised in the session report as a permission-default decision.**
- 2026-08-27 · (M2.7) `InstalledSettingsPlan` reference-counts per settings path; the LAST agent out restores the original · two agents in one repository share one `settings.local.json`, and the first to finish was deleting the file the second was still running under — the survivor lost access to its own mailbox mid-turn. Found by the two-real-agent exit demo; regression tests named for it.
- 2026-08-27 · (M2.7) `prompts/agora/PROTOCOL.md` gained the concrete message format, and `identity.md` the agent's mailbox path · a real agent cannot follow a contract that never tells it where its outbox is or what a message looks like. The exit demo proved the addition works: the agent quoted the rule back ("`requires_reply: true` as protocol mandates for a `request` act").
- 2026-08-27 · (M2 close-out) `Agora.commitSoon()` replaces every `void agora.commit(...)` · `void` attaches no rejection handler, so a commit that exhausted its retry budget became an `unhandledRejection` — a failed vitest run in CI, and a dead Electron main process in production. A git failure is the exact fault ADR-0004's queue exists to absorb; it must never be what takes the company down. The give-up is now recorded (bounded at 50) and reported through `onCommitError`. Found by CI going red on a run where all 581 tests passed.
- 2026-08-27 · (M2 close-out) `onCommitError` carries a written contract that it must not log or queue work · the failing subsystem is git itself, and a handler that commits would recurse. `index.ts` reports it to the same `console.warn` surface hook drift and settings-sweep failures already use.
- 2026-08-27 · (M2 close-out) The watcher's `sweep()` and the pty exit handler got the same treatment (`onSweepError`, `onExitError`) · both are fire-and-forget by nature and both provably reject — three existing tests assert `sweep()` rejects — so both were live harness-killers of the same class, not hypothetical ones.
- 2026-08-27 · (M2 close-out) Each of the three regression tests installs a `process.on('unhandledRejection')` probe AND asserts the failure was recorded · asserting only "no unhandled rejection" would pass vacuously if the failure path stopped firing. No test deliberately creates an unhandled rejection to prove the probe works — that would fail the run it is defending.

- 2026-08-27 · (Architect verdict, M2 close) The M2.7 mailbox permission grant is **RATIFIED as-is** · each agent's `settings.local.json` grants file access to its OWN `agora/agents/<id>/` directory only — the narrowest grant that makes FR-3.2 implementable. Recorded as an Architect-ratified permission default; no memo obligation attached.
- 2026-08-27 · (Architect verdict, M2 close) The M2 demo view stays textual · the exit criterion is a behaviour, not a screen, and the verified transcript is the honest artifact. Embedded in README with status "M2 complete"; a screenshot of the Activity tab is owed from the next local run and noted there.
- 2026-08-27 · (Architect verdict, M3 plan) S-GATE's voice/remote clauses are built as POLICY SEAMS in M3 · source-channel and repeat-back are first-class gate-policy inputs tested with scripted stubs (fake STT transcript, fake remote channel); the Herald (M6) and Harbor (M7) adapters plug into the same seam. S-GATE passes fully in M3 at the policy boundary, per TEST-STRATEGY's own "policy layer test with scripted STT" wording.
- 2026-08-27 · (Architect verdict, M3 plan) FR-5.4 "respawn with memory intact" means ENGINE-NATIVE RESUME in M3 · adapter `ResumeSupport` (claude `--resume` with the session id the event plane records) + re-injected identity/roster context. Library-backed `memory.md` continuity is M4's job; the M1-audit resume capability gap closes inside M3.7 rather than as adapter gold-plating.
- 2026-08-27 · (Architect verdict, M3 plan) Telemetry in M3 is span CAPTURE only · the breaker's repetition/error signals need span data (agent, tool, duration, outcome) anyway, so M3.5 records it; the FR-11.6 waterfall UI waits for a later milestone rather than being invented UI now.
- 2026-08-27 · (Architect verdict, M3 plan) Sheet-based floor rendering and the owed §8 badge double-encoding ride M3.6's floor-layout package · the temple room needs drawing anyway, and gates/breaker make `waiting`/`blocked`/`looping` reachable for the first time — the color-only badge pairs stop being low-urgency exactly there.
- 2026-08-27 · (CI) `check-invariants.cjs` allowlists `scripts/arm-hooks.cjs` and `scripts/check-attribution.cjs` for git invocation · ADR-0004's single-committer rule governs the running app's Agora; these two are development-repo tools that execute outside the app process against this repository itself and can never touch a harness home. The allowlist stays named-files-only — the tripwire caught them the moment they landed, which is the tripwire working, not a false positive of design.
- 2026-08-27 · (M2 close-out audit) The sweep timer and the watcher share ONE guarded tick (`sweepAndWake`) · the close-out fixed `void this.sweep()` on the watcher path and missed the identical defect on the periodic timer — the audit proved it process-fatal with a live probe. One tick, one rejection handler, and the wake watchdog chained on, so there is no second path to forget.
- 2026-08-27 · (M2 close-out audit) `wakeCheck()` runs on every production sweep tick, not on its own schedule · the watchdog had zero production callers — S-WAKE proved the decision logic while the shipped app never ran it. Chaining it onto the sweep means it runs exactly as often as delivery does, which is the only time pending mail can newly exist.
- 2026-08-27 · (M2 close-out audit, Architect verdict) Inbox consumption is HAND-OVER: `decideOnStop` and `wakeCheck` move mail to `inbox/.done/` in the same act that renders its content into the block reason / wake nudge · nothing else ever consumed in production, so a protocol-following agent re-blocked on the same handled mail until the cap. Consumption at hand-over makes "the session has seen it" and "it is archived" the same event; PROTOCOL.md now says handed-over mail is already archived. The alternative (agents move their own mail) was rejected for hinging the R2 pathology on an LLM remembering a chore.
- 2026-08-27 · (M2 close-out audit) `wakeCheck` refuses to run without a nudge sink · consuming mail with nobody to hand it to would archive messages no session ever saw — silent loss dressed as tidiness.
- 2026-08-27 · (M2 close-out audit) `{{messages}}` in the hermes prompt surfaces carries the handed-over messages as verbatim JSON (`formatHandover`) · the data is data; every word of framing stays in `prompts/hermes/` (invariant §8). The no-prompts test fallback is now a mechanical serialization for the same reason.
- 2026-08-27 · (M2 close-out audit, Architect verdict) Data-plane degradations surface through `agora:health` + a status-strip chip · `fileWarnings()`/`commitFailures()` had no consumers and every runtime error callback ended at `console.warn`, which invariant §7 does not accept as "visible". One bounded collector (`reportDegradation`, cap 50) feeds the chip; `console.warn` stays as the developer trace.
- 2026-08-27 · (M2 close-out audit) A schema file that failed to parse refuses overwrite until a later read succeeds · the M2.2 decision promised the corrupt file is "never rewritten", but the first roster write after corruption replaced it with the empty default. The refusal throws; `onRosterChange` reports it as a degradation and the company keeps running.
- 2026-08-27 · (M2 close-out audit) Bounce subject/body render from `prompts/hermes/bounce-*.md` · the refusal is read by an LLM, so it is a prompt surface like the block reason and the nudge (invariant §8). The 200-char subject truncation stays in code — mechanics, not words.
- 2026-08-27 · (M2 close-out audit) `EPH_BLOCK_CAP` is the ADR-0013 env-configurable cap, parsed by `blockCapFromEnv` · only a positive integer wins; junk, zero and negatives are refused visibly and the default holds, so the environment can tune the cap but never disable it.
- 2026-08-27 · (M2 close-out audit) `HookServer.onEvent` may be async, with `onEventError` reporting a handler failure while the engine gets fail-open `{ok:true}` · the Stop decision now consumes the inbox before replying, and an awaited handler that rejected would otherwise be a new member of exactly the unhandled-rejection class this audit closed.
- 2026-08-27 · (M2 close-out audit, recorded not fixed) The router-authored bounce still says `from: <original sender>` · SDD §4.4 gives the router no legal `from` identity, and inventing one is a schema change (§8 must-ask). Flagged for M3, where Artemis becomes the natural author of such refusals.

- 2026-08-27 · (M3.1) The secret store lives at `<harness home>/secrets.enc`, mode 0600 · SDD §2 lists the harness home's contents but predates the broker; the store must sit outside the Agora (never committed, never agent-readable) and outside SQLite (`db.sqlite` is a file the ledger and window state share), which leaves the harness-home root. SDD §2 updated to list it.
- 2026-08-27 · (M3.1) `secrets:test` asks "can the broker still retrieve this?", not "does the provider accept it?" · no provider adapter exists before the Herald (M6) and the Harbor (M7), and a test that pretends to reach a provider it cannot reach is worse than none. The question it does ask is a real failure: an OS keychain that rotated leaves a file of undecryptable blobs whose first symptom would otherwise be an agent spawning without its credential.
- 2026-08-27 · (M3.1) A machine with no OS encryption backend gets a REFUSAL, not a plaintext fallback · ADR-0010 permits "an encrypted file keyed per-machine" as the fallback, which is what `safeStorage` already is; storing plaintext instead would break NFR-8. the broker REPORTS the refusal at construction through `onDegraded` → the status-strip chip, and the store file is never created. Reporting, not a queryable `health()` field: the M2 close-out audit already ruled that a state with no consumer does not satisfy invariant §7, and the first draft of this package repeated that mistake. Proven live: on a box with no keyring, `set` throws and no file appears.
- 2026-08-27 · (M3.1) The redaction filter holds back a trailing region ONLY when it is genuinely a prefix of a known secret · the alternative (a fixed look-back window) would delay the last characters of every prompt an engine draws, on every chunk, forever. With the prefix test, ordinary output is emitted with zero delay and a credential torn across two PTY reads is still caught — proven live with a 0.4 s gap between the halves.
- 2026-08-27 · (M3.1) Values shorter than 4 characters are never masked (`REDACTION_MIN_LENGTH`) · a three-character "secret" has no entropy to protect and masking it would shred every terminal that prints those characters — more damage than the leak.
- 2026-08-27 · (M3.1) `AgentManager` re-scopes whatever `resolveGrants` returns to the declared names before building the env · "undeclared vars never reach a spawn" has to hold at the boundary that composes the environment, not only inside whichever resolver is wired in. Written after a test that fed the manager an over-answering resolver caught the gap.
- 2026-08-27 · (M3.1) `writeFileAtomic` gained an optional `mode`, applied to the TEMP file before the rename · a secret store that is world-readable for even the width of a follow-up `chmod` is a leak.
- 2026-08-27 · (M3.1) `check-invariants.cjs` now implements the two ENGINEERING-STANDARDS §5 tripwires that were documented but unenforced: no credential read from `process.env` outside `src/main/watch/` and `src/main/herald/`, and no secret-shaped string anywhere in `src/`, `shims/`, `scripts/` or `test/` · both proven to bite on planted probes. `EPH_*` is excluded — the harness's own spawn-scoped variables are declared capabilities, not credentials. The secret-shape patterns are prefix-anchored rather than entropy-based: a heuristic that fired on any long random string would flag every hook-token fixture in the suite, and a tripwire nobody trusts gets deleted.

- 2026-08-27 · (M3.1 review) The `secrets:` IPC group is **exactly SDD §5's four channels**; the `list`/`health` channels the first draft added are gone · SDD §5 calls its listing abridged, but neither extra channel had a consumer, and widening a documented IPC signature is a BUILD-PROMPT §8 must-ask rather than an implementation detail. The API-surface test now pins the documented four, so a fifth fails CI whether or not it reads a value. Storage health reaches the Architect through the existing degradation surface instead.
- 2026-08-27 · (M3.1 review) The broker memoizes its decrypted values and invalidates on `set`/`delete` · the redaction filter consults them on EVERY pty chunk for every agent; decrypting per chunk would be N synchronous OS-crypto calls on the main thread at the PTY data rate (SDD §11) and — worse for a write-only broker — a fresh plaintext copy of every credential on the heap just as often. A test counts decrypts across 50 chunks and asserts exactly one.
- 2026-08-27 · (M3.1 review) Grants resolve in `start()`, not in `spawnConfig()` · ADR-0010 injects credentials *at spawn*, and the config is built before the version probe runs. The old placement decrypted credentials for the missing-binary path that deliberately discards them, and — after an install offer finished — reused a stale config, so a credential stored *while watching the install* never reached the agent that followed it.
- 2026-08-27 · (M3.1 review) The stream wiring moved to `src/main/pty-stream.ts`, which imports no native module · with it inside `pty.ts` the only thing standing between an echoed credential and the renderer was covered by a manual live run: deleting the `filter.push` call left all 633 tests green. Eight tests now assert the edge, and four of them fail on exactly that deletion.
- 2026-08-27 · (M3.1 review) `check-invariants.cjs` applies only the SECRET rules to `test/` · the first draft added `test` to the shared search list, silently extending the git and append-only tripwires there too. TEST-STRATEGY §6 explicitly wants integration tests against real git in temp dirs, so that widening would have failed CI for a rule that was never about tests. Proven both ways with planted probes.
- 2026-08-27 · (M3.1 review) `delete` appends `secret-rotated` with `removed: true`, and the `spawn` entry carries grant NAMES · FR-11.4 names set/rotate/**delete** and ENGINEERING-STANDARDS §4 requires anything written into an agent's environment to be logged *and* on the card; the card half held, the log half did not. SDD §4.3's refs are open by design, so neither is a schema change.
- 2026-08-27 · (M3 run topology) Each M3 package branch is cut from the PREVIOUS package's branch, not from `main` · the run's instruction says "cut from main", which presupposes each package merges to `main` as it lands — but `main` is protected (PRs only, one review, ENGINEERING-STANDARDS §2), no PR was authorised for this run, and the packages are genuinely dependent: M3.5's breaker consumes M3.2's burn-rate projection, M3.8's routing consumes M3.7's Artemis, and M3.9's suites consume all eight. Cut strictly from `main`, the chain would stop building at M3.5. Chaining keeps every rule that matters intact — one package per branch, one commit series per package, nothing committed to `main` directly, no PR opened — and leaves the Architect free to merge the chain or cherry-pick per package. **Raised in the session report.**

- 2026-08-27 · (M3.2) The fold cursor counts FACTS ALREADY FOLDED per transcript file, not bytes · `TranscriptReader.read()` returns a whole file's facts, so the ledger folds `facts.slice(folded)`. Re-reading a growing transcript is the common path, not an edge case, and this makes it structurally incapable of double-counting. A file that SHRANK re-folds from zero and reports it: silently skipping a rotated file's first N facts forever is the under-reporting the ledger exists to prevent.
- 2026-08-27 · (M3.2) `costUsd` stays null unless a row reported one; it is never coerced to 0 · Claude Code reports no per-message cost, and a `0` would read as "this was free". The UI must be able to tell "nothing spent" from "not reported" (invariant §7). Deriving dollars would need a price table this milestone has no source for.
- 2026-08-27 · (M3.2) Cache-creation and cache-read tokens count as INPUT tokens · they are tokens the provider billed on the way in; leaving them out would under-report by an order of magnitude (the real transcript folded 72.3M input tokens, of which almost all are cache).
- 2026-08-27 · (M3.2) The burn-rate projection refuses to answer with less than 5 minutes of history · a rate computed from a handful of seconds projects absurd numbers and would trip the breaker on a healthy agent's first tool call. "We do not know yet" (null) is the honest answer, and `because` says which input decided the verdict.
- 2026-08-27 · (M3.2) Spend is folded on a 15 s timer, not per hook event · it is not a real-time quantity, and a fold per tool call would re-read every transcript dozens of times a minute for a figure nobody reads that often (SDD §11). The tick is guarded and non-reentrant, and its rejection handler is attached — the harness-killer class the M2 close-out audit closed.
- 2026-08-27 · (M3.2) `budget` log events fire only on STATE TRANSITIONS · a breached agent emitting an event every tick would turn the book of record into a metronome, and the briefing that reads it would drown.
- 2026-08-27 · (M3.2) `budgetSchema` lives in `src/shared/agents.ts`, re-exported by `registry.ts` · defining it in `registry.ts` and importing it into `agents.ts` is a cycle (`registry.ts` already imports `agentIdSchema` from `agents.ts`), and a cycle in a module zod initializes at import time is a crash, not a style problem — it broke 15 suites before it was moved.
- 2026-08-27 · (M3.2) The conformance suite's transcript case takes a per-adapter SAMPLE instead of one hardcoded format · the existing case wrote the fake engine's JSON and asserted every adapter read it, so the moment the claude adapter declared `transcripts` it "failed" for parsing its own engine's format correctly. Every engine writes its own (NFR-12); what conformance owns is the behaviour ADR-0009 specifies — missing file yields nothing, unrecognized lines are skipped, a good line yields exactly what it said.
- 2026-08-27 · (M3.2) `SpawnRequest` gained an optional `budget`, mirroring `registryEntrySchema.budget` · FR-11.2 enforces per-agent budgets and the hire is where one is declared. Optional exactly as the registry's is: an unbudgeted hire is legal and reports `unbudgeted`, never a zero the Watch would immediately breach.

- 2026-08-27 · (M3.3) A gate rule can only PERMIT; there is no deny rule · denial is the default, and a deny rule would immediately raise "which rule wins" — the ordering bug deny-by-default exists to avoid. The policy shape is the smallest that serves UC-08 and FR-11.1's four named classes plus the two choke-point classes; no policy language was invented.
- 2026-08-27 · (M3.3) A spend rule with NO cap permits nothing · an uncapped spend allowance is an allowance nobody actually wrote, and reading it as "unlimited" would be the most expensive possible misreading of a config typo. Same for a spend request that does not state an amount: it cannot be under any cap.
- 2026-08-27 · (M3.3) A rule permits the `local` channel unless it names others · NFR-9 treats remote and voice approvals as different acts from a click at the keyboard, so neither is implied by a local allowance.
- 2026-08-27 · (M3.3) Refusing a voice approval for want of repeat-back leaves the gate OPEN; it is not a denial · "we could not confirm you meant it" and "no" are different answers, and collapsing them would teach the Architect that the microphone denies things at random.
- 2026-08-27 · (M3.3) The policy REQUIRES repeat-back; the surface that took the approval PERFORMS it · only that surface knows whether the human actually repeated it back. That split is the seam the Herald plugs into in M6 and is why S-GATE's voice clause is testable now with a scripted stub (Architect decision).
- 2026-08-27 · (M3.3) A `gate-policy.json` that fails to parse composes as deny-all, with a visible warning · the safe direction for a parse error to fail in. Proven live: a corrupt file leaves `autonomy=manual rules=0` and reports why.
- 2026-08-27 · (M3.3) `notification` joins `HOOK_EVENTS`, and the claude adapter maps `Notification` to it · the M1 carried item. Its payload schema is loose like the rest: a notification with no message still opens a gate, because "the engine is waiting on you" is the fact that matters and losing it to a missing field is the stall the event exists to end.
- 2026-08-27 · (M3.3) `packageNotification` fills UC-08's four fields honestly rather than inventing a risk assessment · the engine says only that it is waiting and roughly what about. The blast radius of an action the engine has NOT performed is exactly "whatever it was about to do", and the rollback for a refusal is that it never happens.
- 2026-08-27 · (M3.3) A `needs_human` message is delivered AND gated · FR-3.3 forbids escalation that swallows mail; the gate is about the decision behind the message, not about the message.
- 2026-08-27 · (M3.3) Only the LAST gate clearing on an agent returns its avatar to its prior state · an agent held behind two gates must not walk back to its desk after the first verdict.
- 2026-08-27 · (M3.3) The `gate:open` push carries the gate that opened (or null on a verdict) as a NUDGE; the renderer re-reads `watch:approvals` · a push carrying the queue would become a second copy that could disagree with main. Same rule the Activity feed follows.
- 2026-08-27 · (M3.3) Gate prose lives in `prompts/watch/` · what an agent or the Architect reads about a held action is a prompt surface like the block reason and the bounce (invariant §8). The gate itself carries a machine-readable `because`. **Corrected at review: the first draft created the prompt files and never loaded them while the packaging prose sat inline in `.ts` — the entry claimed compliance the code did not have. The packaging now renders from `prompts/watch/packaging-*.md`.**

- 2026-08-27 · (M3.2 review) The Watch folds only the transcripts belonging to session ids the event plane recorded, and the fold cursor is keyed `(agent, source)` · an engine keys its transcript directory on the working directory, not on the agent. FR-1.5 makes worktree isolation optional, so a shared `cwd` is the documented default — and the first draft folded the whole directory, attributing two agents' spend (and the Architect's own `claude` history in that repo) to whoever ticked first. Mis-attribution is the exact reason ADR-0011 rejected provider-side caps. **An empty session list folds nothing**: recording nothing until we know whose it is beats recording it as ours.
- 2026-08-27 · (M3.2 review) `UsageFact` carries the engine's timestamp, and `day` is the day of SPEND · the ledger's `day` column IS the budget window (SDD §4.6 with registry §4.1). Folding on the harness's clock billed an old transcript's tokens to today — an agent in a previously-used repo breached on its first tick from history alone — and billed an agent running across midnight to tomorrow. ADR-0009 names the `transcripts` member, not the fact shape, so this is a §8.2 mechanical choice rather than a §8.3 schema deviation; recorded here because it changes what a documented column means.
- 2026-08-27 · (M3.2 review) The burn-rate window carries its own spend baseline · `spent` is durable (all of today, across restarts) while `elapsedMinutes` can only measure this process's uptime. Dividing the first by the second projected a healthy agent straight into `projected-breach` after any restart or mid-day spawn — the false trip ADR-0011 warns about, and the 5-minute floor could not prevent it because the *origin* was wrong, not the sample size.
- 2026-08-27 · (M3.2 review) An agent's transcripts are folded ONE last time on exit, before its session is forgotten · usage written in the seconds between the final tick and the exit would otherwise never be folded, and that spawn is never `running` under that session again. Under-reporting is the ledger's one unforgivable failure.
- 2026-08-27 · (M3.2 review) The enforced budget comes from the ROSTER when the spawn request omits one · the renderer is untrusted (invariant §2), and taking the ceiling from whatever it supplied meant a hire whose registry entry declared 500k was unbudgeted the moment a spawn call left the field out, with a harness restart losing the declaration entirely.
- 2026-08-27 · (M3.2 review) `AgentSpend.reporting` distinguishes "this engine cannot report usage" from "this agent spent nothing" · ADR-0009 makes `transcripts` optional, so the first is a visible product tier; a bare zero for both is the silent fallback invariant §7 refuses.
- 2026-08-27 · (M3.2 review) `check-invariants.cjs` gained a tripwire for `UPDATE`/`DELETE FROM cost_ledger`, with no allowlist · the ledger became a SQL table in M3.2, so the rewrite vector is no longer a `writeFileSync` the old pattern could see. `cost_fold_cursor` is deliberately not covered — it is metadata about reading, and it is meant to be updated. Proven to bite on both statements.
- 2026-08-27 · (M3.2 review) `COST_SCHEMA_VERSION` deleted rather than persisted · invariant §9 versions schema'd *files*; the ledger is a SQLite table governed by migration on open, and a constant nothing writes was the appearance of compliance with nothing behind it.
- 2026-08-27 · (M3.2 review) `MemoryLedgerStore` is a TEST DOUBLE only; its doc no longer offers it as a runtime fallback · cumulative spend in memory is exactly what invariant §11 forbids, so a harness without SQLite must fail visibly rather than keep totals a restart erases.
- 2026-08-27 · (M3.2 review, plan) FR-11.2's UI clause had no owner in the M3 plan and is assigned to M3.4 · `watch: budgets()` shipped with no consumer and M3.8's "Ledger tab" is the task kanban (FR-4.3), not cost. The Watch panel is the Watch's surface; the strip shows the two figures the requirement names and nothing more. **Raised in the session report.**

- 2026-08-27 · (M3.4) `watch: humanQueue()` is added to the `watch:` group · SDD §5 calls its listing abridged and names the generated `.d.ts` as normative once code exists, so filling in a channel the design requires is not a deviation. The test the M3.1 review taught: a channel is legitimate when something *consumes* it and a requirement demands it — here the M2 carried item (`agora/human/` accumulating unread) and FR-3.7. `secrets:list`/`secrets:health` failed that test and were removed; this passes it.
- 2026-08-27 · (M3.4) The approvals panel re-reads `watch:approvals` after every verdict instead of editing its own copy · the renderer is a projection (invariant §2). A panel that removed the row itself would be holding gate state, and would disagree with main the moment a verdict was refused.
- 2026-08-27 · (M3.4) A refused verdict is SHOWN in the panel, not swallowed · "we could not take that verdict" — a voice approval never repeated back, a gate that settled while the row was on screen — is exactly the state the Architect must see (invariant §7).
- 2026-08-27 · (M3.4) `gateApproveSchema` lives in `src/shared/gates.ts`, not inline in `ipc.ts` · ENGINEERING-STANDARDS §3 puts validators in `src/shared/`, next to the types they validate.
- 2026-08-27 · (M3.4) `humanQueue()` skips a file it cannot parse rather than failing the whole queue · one torn message must not make the rest of the Architect's mail invisible. The sweep already parks and reports malformed mail; a reader's job is to read what is there.
- 2026-08-27 · (M3.4) Budget state is rendered as a WORD, not a colour · UI-DESIGN §8 and NFR-15 require status double-encoding; `breached` reads as `breached` in a screenshot with no colour at all.
- 2026-08-27 · (M3.4) An engine that reports no usage renders `—` and "engine reports no usage", never `0 tok` · a zero from an engine that cannot report and a zero from an agent that spent nothing are different facts (invariant §7).

- 2026-08-27 · (M3.3 review) NFR-9 is enforced on the VERDICT, not only on the request · `requiresRepeatBack` was derived from the hold reason, which under deny-by-default is almost always `no-rule` — so the flag was false on exactly the destructive ops the clause protects. And `decide()` never consulted `rule.channels`, so a remote approval of a gate the policy never opened to remote was taken. A gate held under deny-by-default matched no rule by definition; checking the channel only on the way in left NFR-9 binding on nothing at all. `checkVerdictChannel` now polices the approval side, and `repeatBackRequired` derives from the gate's own facts, with destructive ops always requiring repeat-back by voice.
- 2026-08-27 · (M3.3 review) A rule at `autonomy: 'manual'` permits nothing, and an effective level of `manual` permits nothing · the file already said so in prose; the rank comparison let `manual === manual` fall through to allow, which left ADR-0012's tightening with no floor. FR-11.1 reads `manual` as "I approve this by hand".
- 2026-08-27 · (M3.3 review) `tool-permission` is never permittable, whatever the policy says · "allow" is not a meaningful verdict for the engine's own dialog: the harness has no action to permit, the engine is blocked on a human, and a policy that answered it would silently restore the invisible stall the M1 carried item was about (invariant §7). The stall is also reported as a degradation, independent of the gate.
- 2026-08-27 · (M3.3 review) `wireGateChokePoints` is the single implementation of SDD §9's three choke points, called by both `index.ts` and the scenario rig · the rig had copied the wiring character-for-character, so S-GATE would have stayed green with the production choke points deleted — the same defect class the M3.1 review caught in `pty.ts`. Proven by planting exactly that defect: 6 tests across both suites now fail.
- 2026-08-27 · (M3.3 review) Repeated submissions coalesce per (agent, kind), keeping the FIRST packaging · engines emit notifications repeatedly, and a gate per event would bury the queue and turn `log.jsonl` into a metronome. The first packaging is kept because that is the question the Architect is already being asked; swapping it mid-decision is worse than a duplicate.
- 2026-08-27 · (M3.3 review) Gate ids carry 64 bits of suffix, not 16 · under a shared clock, twenty gates had ~0.3% odds of a collision, and a collision silently overwrote a still-open gate *after* its `onOpen` had fired — a held action vanishing from the queue.
- 2026-08-27 · (M3.3 review) Two rules for one kind are refused by the schema AND resolved strictest-first by the matcher · either alone would do; both means an Architect who wrote two by accident is told rather than quietly overruled, and the matcher never depends on which was written first.
- 2026-08-27 · (M3.3 review) The spend cap is `maxSpendTokens`, not `maxSpendCents` · the durable ledger reports tokens (the engine reports no per-message cost), so the cents field was being compared against a token count and the policy knob was silently meaningless. It becomes a currency cap when an engine reports one.
- 2026-08-27 · (M3.3 review) `gate:open` carries the gate ID, not the gate · both the channel's own doc comment and the M3.3 log entry said it was a nudge and that the renderer re-reads `watch:approvals`; the code sent the whole `OpenGate`, which is the second copy of the queue that contract exists to prevent.
- 2026-08-27 · (M3.3 review) The gate-policy warning reports on CHANGE, not on every evaluation · the policy is re-read per evaluation, so a corrupt file plus a chatty agent evicted every other entry from the bounded health buffer — the opposite of what invariant §7 asks for.
- 2026-08-27 · (M3.3 review) `gate-policy.json` is documented in SDD §2 · a new schema'd file at the harness-home root, and the first draft added it without recording the choice. It sits beside `config.json` rather than inside it because a policy the harness cannot read must compose as deny-all *without* taking the rest of the config down with it.

- 2026-08-27 · (M3.5) The breaker's signals are pure functions over spans the caller supplies; nothing in `src/shared/breaker.ts` reads a clock, a file or a process · a trip has to be explicable after the fact rather than a mood the harness was in, and that means the same inputs always producing the same verdict.
- 2026-08-27 · (M3.5) Repetition means the same tool AND the same argument fingerprint · an agent reading twenty different files is working; an agent reading one file twenty times is stuck, and only the fingerprint tells them apart. A breaker that fires on the first is one the Architect turns off, and then it protects nothing.
- 2026-08-27 · (M3.5) Spans carry a DIGEST of the call's arguments, never the arguments · spans are local-only (NFR-10) but they are also read by the briefing compiler, and a tool call's arguments can contain anything the agent was working on. Asserted by a test that plants a path and a secret-shaped value and greps the serialized spans.
- 2026-08-27 · (M3.5) The error signal ignores spans still open · an open span has no outcome yet, and counting it either way would be a guess.
- 2026-08-27 · (M3.5) A **rung dwell** (60 s default) gates every climb after the first · without it the ladder climbed once per tool call, so an error storm reached `stop` three calls after the floor and the steer — queued until the agent is idle (FR-1.3) — may not even have been delivered. A ladder that climbs three rungs in three seconds is the kill switch ADR-0011 explicitly is not. Rung 1 is never delayed, and recovery is never delayed: an agent that stopped misbehaving should not serve out a sentence. **Found by the S-BREAKER scenario, not by review.**
- 2026-08-27 · (M3.5) Rung 2 HOLDS mail in the sender's outbox rather than dropping it · ADR-0011 says "pause its Hermes inbox deliveries"; constraining an agent is not the same as losing its mail, and the held message arrives when the pause lifts.
- 2026-08-27 · (M3.5) The breaker evaluates on span CLOSE, not on a timer · the signals are about tool calls, so a tool call finishing is exactly when the answer can have changed. The dwell, not the cadence, is what paces the ladder.
- 2026-08-27 · (M3.5) `Breaker.forceEvaluate` exists for scenarios only and has no production caller · a scenario asserting the ladder must be able to step it without sleeping a minute per rung; the dwell is the property, not an obstacle to it.
- 2026-08-27 · (M3.5) Every rung's corrective sentence is a file in `prompts/watch/steer-*.md` · the breaker supplies facts (`{{tool}}`, `{{repeats}}`, `{{errors}}`) and every word is config (invariant §8). The template is chosen by the signal tag, which is data.
- 2026-08-27 · (M3.5, bug fix) Bundled pixel fonts are fetched by RELATIVE path · a packaged app loads its renderer over `file://`, where `/fonts/x.woff2` resolves to the filesystem root and `fetch` fails outright — so the built app had never loaded a face and showed "3 of 3 pixel faces missing" permanently, while the dev server's http origin made it look fine. A degradation warning that is always on is worse than none: it trains the Architect to ignore the surface every other degradation shares. Found by reading an M3.5 evidence screenshot; regression test resolves the path against both a `file://` and an `http://` base.

- 2026-08-27 · (M3.6) A seat is `temple` or `terrace-<n>`, assigned by a pure function over the seats already taken · SDD §4.1 writes both by example, and the two properties that make a seat a *place* — an agent keeps it, and the temple is Artemis's — are only testable if assignment has no clock, no randomness and no dependence on the order the roster is read in.
- 2026-08-27 · (M3.6) A seat that does not parse is REASSIGNED, not rejected · every roster written in M1 and M2 says `seat: 'terrace'`, and tightening `registryEntrySchema.seat` from `string` would have turned all of them into unreadable rosters. The floor also draws an unparseable seat rather than crashing on it.
- 2026-08-27 · (M3.6) `AgentManager` remembers the seats it hands out, on top of the roster it is given · the roster is the durable memory, but it is written *by* the result of the assignment, so two hires in the same tick would otherwise both be handed `terrace-1`. Asserted by a test that starts two spawns before either roster write lands.
- 2026-08-27 · (M3.6) A worker holding the temple seat in the roster is turned out of it · a file written by hand, or by an older harness, is not authority to sit in the temple (ADR-0005). The reserved seat is reserved by the assignment rule, not by everyone else's good behaviour.
- 2026-08-27 · (M3.6) `role === 'orchestrator'` is the single predicate for who owns the temple, exported as `isOrchestratorRole` · the roster's `orchestratorId` is not set until Artemis is hired (M3.7), and the floor already read the same role string for her silhouette. One predicate, so the two cannot drift.
- 2026-08-27 · (M3.6) The floor's layout is a pure `floorPlan()` of typed cells, and art *paints* it · ADR-0014 requires the floor to render only from event-plane data and never become a second source of truth. UI-DESIGN §7's licensed tileset is exactly the thing that could quietly invert that — a station existing because a tile was drawn there. With the plan in front, installing a pack changes how the floor looks and cannot change what is on it; the same run with and without a pack produced identical seats, phases and census.
- 2026-08-27 · (M3.6) A tileset pack ships its own `*.tiles.json` tile map, validated by `src/shared/tileset.ts` · every pack lays its sheet out differently and the sheets stay out of the repository (§7 licence rules), so hard-coded frame indices would be fiction: they would compile, look deliberate, and paint whatever sat at those offsets. `station:<name>` overrides the generic `station` key, and an unmapped cell falls back to the procedural tile so a partial pack cannot punch holes in the floor.
- 2026-08-27 · (M3.6) A sheet with no map, an invalid map, or a map naming a missing sheet all leave the floor procedural AND say which · a tileset that quietly failed to load would look exactly like one nobody had installed yet (invariant §7).
- 2026-08-27 · (M3.6) The tile map refuses a `tilePx` that does not divide the 32px world tile · UI-DESIGN §7 permits "integer scaling only, pixel-snap preserved"; a validator is where that stops being a sentence.
- 2026-08-27 · (M3.6) Every status badge carries a GLYPH beside its colour, and the canvas carries a census in words · UI-DESIGN §8 requires status double-encoding, and three pairs of phases share a status colour (`alert`/`thinking`, `ghost`/`archived`, `blocked`/`stopped`). M3 is also what makes `waiting`, `blocked` and `looping` reachable at all. The census exists because a `<canvas>` is opaque to a screen reader: without it, §8's double encoding stops at the glyph.
- 2026-08-27 · (M3.6) An agent seated past the drawn desks is reported as "sharing a desk" rather than silently stacked · a hire is never refused a place, and an overlap the Architect has to notice by eye is the kind of degradation invariant §7 exists to forbid.
- 2026-08-27 · (M3.6, bug fix) The tile sheet is decoded through the DOM (`Image.decode()`), not through Pixi's asset resolver · the resolver picks a parser from the URL's *extension*, and the bundler inlines a small sheet as a `data:` URL, which has none — so an installed pack fell back to procedural with a loader error. Found by installing a pack in a live run rather than by review. Nothing about a tileset should depend on the shape of the URL it arrived on; the texture is built with `scaleMode: 'nearest'`, since §7's integer upscale is only integer if nothing interpolates it.

- 2026-08-27 · (M3.7) `artemis.ts` holds lifecycle and nothing else — hire, seat, policy text, respawn, and one question ("may she settle this?") answered from a table the Architect wrote · FR-5.1 is the constraint the module is written against. If a rule about how work is decomposed, who gets which task, or what counts as critical ever appears in this file, the mechanism/intelligence split ADR-0005 rests on has been lost. Asserted by a test that grants everything and shows the harness allows everything: it has no opinion of its own to override the Architect's with.
- 2026-08-27 · (M3.7) Her system prompt reaches her through a general `roleBrief` seam on `AgentManager`, not a special case for the orchestrator · the manager renders the identity, appends whatever the hirer supplied, and writes the file — it never reads the text. A hire template's role brief uses the same seam in M4.
- 2026-08-27 · (M3.7) A respawn mints a FRESH hook token · `spawnConfig` already said a token is "minted per spawn and never reused: a token that outlived its process would let a dead agent keep writing the event plane". A respawn is a new process, so it gets a new token; asserted rather than assumed.
- 2026-08-27 · (M3.7) The respawn ladder resets only after a **stability window** (60 s default), not on every successful start · the first draft reset the counter whenever the agent reached `running`, which meant the ladder could never be spent by the one failure it exists to bound — a process that starts and immediately dies would be respawned forever. Recovery is coming back and *staying* back. **Found by a test, not by review**: the give-up case failed with five spawns where three were expected.
- 2026-08-27 · (M3.7) Giving up is loud: a degradation, an `orchestrator` log line, and `orchestratorId` cleared · a company with no orchestrator is exactly the state the Architect must be told about (invariant §7), and a roster that still names a dead agent as orchestrator is worse than one that names none.
- 2026-08-27 · (M3.7) `log` gains the kind `orchestrator` (SDD §4.3 updated) · FR-5.5 requires everything Artemis decides under delegated authority to be auditable afterwards, and no existing kind carried a countersignature, an escalation, or a respawn-ladder rung. The countersign *surface* — filing into the memo/gate archive — is still the Odeon's (M5).
- 2026-08-27 · (M3.7) The delegated-authority table lives at `<home>/authority.json`, beside `gate-policy.json` (SDD §2 updated) · same posture as the gate policy: absent or unreadable ⇒ **nothing** delegated, everything escalates. A file the harness cannot read must never be the reason authority widened. Re-read per decision rather than cached, because the Architect edits it while the company runs; the parse failure is reported once per distinct reason so one bad file plus a busy company cannot flood the health buffer.
- 2026-08-27 · (M3.7) The permission and the countersignature are the same call · `mayDecide` returns the countersignature *with* the allowance, so there is no code path that takes a decision under delegated authority without leaving the Architect something to audit (FR-5.5).
- 2026-08-27 · (M3.7) A grant with no domains is a parse error, not "all domains" · FR-5.5's authority is per domain; a rule that forgot its domains should grant nothing rather than everything. Likewise a `spend` grant with no `maxSpendTokens` is refused by the schema, and refused again by `mayDecide` if one is constructed anyway — absent is never unlimited.
- 2026-08-27 · (M3.7) A spend decision that names no amount is refused · "how much?" unanswered is not a small spend; it is an unknown one.
- 2026-08-27 · (M3.7) `mayDecide` has no production caller yet; M3.8's routing is the first · the plan assigns the countersign surface to the Odeon and Artemis's proxy role to M3.8, so what M3.7 owes is the table, its validator and the enforcement hook every future caller goes through. Recorded rather than hidden, on the same footing as M3.5's `forceEvaluate`.
- 2026-08-27 · (M3.7) `claude --resume <sessionId>` closes the M1-audit gap · `ResumeSupport` was in the adapter type from M1 with no adapter implementing it and nothing calling it, so a crashed agent came back with no idea what it had been doing. The session id comes off the event plane exactly as the interface's doc comment always said it would, and the log records whether memory actually carried over — an engine with no resume still respawns, with a fresh session, and says so rather than implying a continuity that is not there.

- 2026-08-27 · (M3.8) The ledger endpoint is addressed as a **reserved agent id** (`agent.ledger`), not as a new `to` value · SDD §7.1 says Artemis writes the ledger via "files in its own outbox as `propose` to harness ledger endpoint" but names no address, and §4.4's `to` domain is `agentId | "broadcast" | "human"`. Adding a fourth literal would be a deviation from a normative schema (a §8 must-ask); a reserved agent id uses the first branch exactly as documented. Surfaced in the session report so the Architect can promote it to a `"ledger"` literal in §4.4 if they would rather.
- 2026-08-27 · (M3.8) `agent.hermes` and `agent.ledger` are reserved: `spawnRequestSchema` refuses them · otherwise a hire could take `agent.hermes` and forge a router refusal in the harness's name.
- 2026-08-27 · (M3.8, closes a recorded gap) The router-authored bounce is now `from: agent.hermes` · the M2 close-out recorded that it claimed `from: <the original sender>` — a message the sender never wrote, attributed to them — because §4.4 gave the harness no legal identity, and flagged it for M3 "alongside Artemis's proxy role". Reserved agent ids give the harness a legal `from` with no schema change.
- 2026-08-27 · (M3.8) The writer check lives in `routeMessage`, not in the endpoint · ADR-0003 calls the addressing rules "transport rules, not etiquette". By the time a message reaches the endpoint the router has already established that the orchestrator sent it, and "agents never touch tasks.json" is enforced on the only way in.
- 2026-08-27 · (M3.8) A proposal applies whole or not at all, and every reason comes back at once · a half-applied decomposition is a ledger nobody can reason about, and a refusal Artemis can act on in one pass beats one that reveals the next problem only after she fixes the first.
- 2026-08-27 · (M3.8) `→ done` is judged against the task AS IT WILL BE · a proposal that adds a result ref and closes in the same op satisfies SDD §4.2's rule; judging the opening state would refuse it.
- 2026-08-27 · (M3.8) A create with an unreachable assignee is refused · a task addressed to an agent with no mailbox is work that will never be asked for, and the ledger would claim it was assigned.
- 2026-08-27 · (M3.8) Dependencies are checked once, after every op · a proposal may legitimately create a task and its dependant in the same breath.
- 2026-08-27 · (M3.8) `board.md` travels through the same endpoint as the ledger · FR-4.2's "single scribe = Artemis" is only enforceable if the write goes through the harness; a file agents could write directly would make the rule a comment. The board is seeded from `prompts/agora/board.md` so the tab has something honest to show.
- 2026-08-27 · (M3.8) `pendingTasksFor` counts `todo` and `in_progress` only · ADR-0013's branch asks "should this agent keep going?", and an agent whose only tasks are `blocked` or `stalled` should stop, not be told to continue. `review` is owed to the Odeon, not to the assignee.
- 2026-08-27 · (M3.8) Task ids are `t-<date>-<random>`, not a counter · two proposals in flight would otherwise mint the same id, and a counter read from the ledger is a read-modify-write nobody holds a lock on.
- 2026-08-27 · (M3.8) The kanban cannot write · tasks are Artemis's to propose and the endpoint's to apply (SDD §7.1); a board the Architect could drag cards on would be a second writer to a file ADR-0004 gives exactly one. The panel re-reads on a `state:tasks` nudge rather than editing its own copy.
- 2026-08-27 · (M3.8, bug fix) The Ledger panel carries `minWidth: 0` · a flex child defaults to `min-width: auto` ("never shrink below my content"), so six kanban columns pushed the terminal pane to a sliver and gave the whole window a horizontal scrollbar. Found by reading the evidence screenshot; the columns now scroll inside the panel.

- 2026-08-27 · (M3.9) The fake engine gains a `write-transcript` step · S-LEDGER must fold a transcript an *engine* wrote, in the engine's own format and place, not one the test hand-placed — otherwise the suite proves the reader and not the pipeline. The fake's format is the one `makeFakeAdapter`'s `TranscriptReader` already reads.
- 2026-08-27 · (M3.9) A company mints a FRESH session id per hire; passing one models `--resume` · a restart without engine-native resume gets a new engine session, so "this session's spend" restarts at zero while the day's does not. S-LEDGER asserts both cases, because M3.7 made the resumed one reachable.
- 2026-08-27 · (M3.9) S-LEDGER's durable plane is the `LedgerStore` seam, not SQLite · `better-sqlite3` is Electron-ABI and cannot load under the Node runner (BUILD-PROMPT §10). What the seam cannot prove is SQLite's own durability; what it does prove is the thing that actually regressed upstream — the harness keeping its own copy — and there is a dedicated test that counts store reads to pin it.
- 2026-08-27 · (M3.9) S-SECRETS injects a grant into the CHILD's spawn environment, never into the harness's own · the first draft set `process.env` before spawning and the invariant tripwire caught it: reading a credential from `process.env` outside the broker is exactly what ADR-0010 forbids. `runTurnIn` takes an `env` map, the way a spawn plan carries one.
- 2026-08-27 · (M3.9) The exit demos run REAL processes with SCRIPTED words · the `claude` binaries in this environment are unauthenticated and sit at a login prompt, so what they would *say* cannot come from them. Every process, socket, router, endpoint, gate, committer and IPC in the demos is the real one; the agents' messages are written into their real outboxes and routed exactly as composed ones would be. Said plainly rather than implied, because a demo that overstates what it proved is worse than one that proves less.

- 2026-08-27 · (M3 close) `main` was fast-forwarded to the M3.9 tip and pushed directly, on the Architect's explicit instruction · the nine package branches form one linear chain, so the merge is a fast-forward with no merge commit and no conflicts, and all twelve `feature/*`+`fix/*` branches are contained in the tip. CI run 33068981836 is green on `main` at `0652cf6`.
- 2026-08-27 · (M3 close, finding) ENGINEERING-STANDARDS §2's "`main` is protected: PRs only" is a policy with no mechanical backing · this run treated it as binding and never pushed to `main` until instructed; the direct push then succeeded, which means no GitHub branch-protection rule enforces the sentence. Recorded rather than quietly relied on: a rule nothing enforces is exactly the class this project refuses elsewhere. Either the protection rule gets turned on or §2 gets softened to say what is true — an Architect call, not an implementer's.
- 2026-08-27 · (Architect verdict, M3 close) The `agent.ledger` reserved-id addressing is **RATIFIED** as the standing rule for harness-owned endpoints · SDD §4.4's `to` domain stays untouched; a harness endpoint is addressed as a reserved agent id the roster refuses to hire, exactly as the bounce-`from` gap was closed. Promoting a `"ledger"` literal was offered and declined.
- 2026-08-27 · (Architect verdict, M3 close) CI runs on `feature/**` and `fix/**` pushes from M4 on · the M3 run's "CI green" was the job set run locally because the workflow only fired on main; per-package green should be a real run. Costs Actions minutes; the Architect accepted the trade.
- 2026-08-27 · (Architect verdict, M3 close) The two M4 engines are **codex + gemini** · IMPLEMENTATION M4 offers codex/gemini/opencode, pick two; the two most-used CLIs after claude give the conformance suite the most real-world coverage. Grades stay honest: whatever each demonstrates is what it declares.
- 2026-08-27 · (Architect verdict, M3 close-out audit) The agent↔task binding join is **carried to M5** · nothing binds a live spawn to a ledger task, so gate→`task.gates` population and rung-3 "task returns `stalled` with the breaker report" (ADR-0011) are unreachable in production; both become load-bearing with the Odeon's task-close gates, which is where the join lands. The M3.8 record's claim was corrected.
- 2026-08-27 · (M3 close-out audit, Architect verdict) Breaker rung 2 now lowers the constrained agent's budget: `CONSTRAINED_BUDGET_FACTOR = 0.5` · ADR-0011 names three constrain effects; mail-pause alone had shipped. The factor applies in the budget watcher's agent view and lifts with the rung — the append-only ledger is never touched. Read-only tools remain excused ("where the engine supports it").
- 2026-08-27 · (M3 close-out audit) Trip signal #3 rides a new `onDiverted` callback, fired once per diverted message · the old wiring sniffed `onBounced` for "hop", but a hop cap is a divert and no bounce reason says "hop" — `noteHopCap` was unreachable. Proven by a divert-not-bounce regression test at the Hermes boundary.
- 2026-08-27 · (M3 close-out audit) Broadcast delivery is per-recipient under a rung-2 pause · one paused recipient used to hold every copy, and each sweep re-delivered the served recipients — duplicate `delivery`/`delivery-held` log entries drumming until recovery. Served copies are now single-shot (inbox/.done existence check), the hold logs once, and the outbox drains when the last recipient is served.
- 2026-08-27 · (M3 close-out audit) `boot()` is a named function with a `.catch` that shows an error box and quits · `void app.whenReady().then(async …)` wrapped the entire boot (including the native SQLite open) with no rejection handler — the M2 fire-and-forget class, at the one place a rejection means "no app at all". A dead boot must say so, not hang silently.
- 2026-08-27 · (M3 close-out audit) The ledger endpoint's `agree`/`refuse` replies render from `prompts/hermes/ledger-*.md` · they are read by Artemis, so their framing is a prompt surface (invariant §8); the refusal reasons stay data in the `{{reasons}}` slot.
- 2026-08-27 · (M3 close-out audit) `to:"human"` from the orchestrator routes to `agora/human/`, never back to her own inbox · the proxy cannot proxy for herself; her escalations were self-delivering, which would swallow exactly the messages FR-3.7 exists to surface.
- 2026-08-27 · (M3 close-out audit) The notification-hook degradation reports once per blocked episode · engines repeat `notification` while one dialog stands, and each repeat consumed a slot in the 50-entry health buffer. The gate itself already coalesced; now the report does too.
- 2026-08-27 · (M3 close-out audit) `packaging-spend.md` states the real verdict mechanics · it promised "approving raises the ceiling for today only", which nothing implements; UC-08 packaging must be honest, so the prompt now says what actually happens (deny refuses the request; approve proceeds; ceiling unchanged).
- 2026-08-27 · (M3 close-out audit) `prompts/watch/gate-{held,approved,denied}.md` removed · loaded by nothing — the agent-facing hold/verdict notification they were written for has no code path yet (the orphaned-prompt class the M3.3 review itself named). They return with the path that loads them.
- 2026-08-27 · (M3 close-out audit) `watch:dismiss` added to SDD §5 · it had widened the documented IPC surface without the doc line + log entry `watch:humanQueue` got; by the repo's own M3.1 rule that pair is mandatory.
- 2026-08-27 · (M3 close-out audit, recorded not fixed) SDD §5's `breaker:trip` push stays unimplemented (the Watch panel polls at 3 s) · owed to a later Watch pass; the panel is correct, just not push-driven. Also recorded: pty-heuristic reduced protection surfaces in the Watch panel rather than on the agent card (placement), and the real `AppDb` remains covered by live runs only (Electron-ABI under vitest).

- 2026-08-27 · (M4.1) The memory layer reaches the adapter as **text on the spawn config**, not as a path · `AgentSpawnConfig.memory` is composed and budgeted in `library.ts` and handed over exactly the way `envGrants` are resolved in main and handed over (ADR-0010's pattern). A `memoryPath` would have put "how much of a long memory fits" inside whichever adapter happened to run — an engine-specific answer to a Library question (NFR-12).
- 2026-08-27 · (M4.1) The injection budget is 8 000 characters of whole sections, newest first · a budget has to exist (`memory.md` grows without bound until reflection lands in M4.4) and half a learning is worse than none, because the agent cannot tell it is reading half. When it bites, the agent is told so from `prompts/library/memory-elided.md` — a silently truncated memory would leave it confidently wrong about what it knows (invariant §7).
- 2026-08-27 · (M4.1) The seed preamble is never injected and never counted · found by a test: with the preamble counted, a hire that had remembered nothing still reported `memoryCarried: true`. That log fact is the M3.7 carried item and has to be true, so "nothing written yet" comes back as nothing. The preamble also duplicates PROTOCOL.md's "How you remember", which the agent already has.
- 2026-08-27 · (M4.1) SDD §10's crash **note** goes to `log.jsonl`, not into the task · `taskSchema` is `.strict()` with no notes field, and widening a normative schema to record a crash would be a §8 deviation. The book of record is where every other harness fact about an agent already lives, and NFR-13 asks for exactly this. Same reasoning as the `agent.ledger` reserved-id call the Architect ratified.
- 2026-08-27 · (M4.1) Only `in_progress` tasks come back to `todo`, and they keep their assignee · a `blocked` task is waiting on something the crash did not change and `review` is owed to the Odeon, so neither is the dead agent's to return; and unassigning would lose the fact that this agent is the one who knows about the task, while the same row offers its respawn.
- 2026-08-27 · (M4.1) A deliberate kill takes the same crash path as a SIGKILL · nothing distinguishes them at the pty seam, both leave assigned work unworked, and `exitCode` is recorded so a reader can tell them apart without the harness guessing.
- 2026-08-27 · (M4.1) `ghost ──30s──► archived` is driven by the avatar clock, mirrored into the roster · SDD §6 already owns the timer; a second timer in the lifecycle would be two planes that can disagree about when an agent stopped being a ghost. `AgentManager.archive()` is the mirror, called from the avatar director's `onChange`.
- 2026-08-27 · (M4.1) S-CRASH runs real child processes through a `ProcessSpawner` test fake · `PtyManager` cannot load under vitest (Electron ABI, BUILD-PROMPT §10.3), and the one thing this scenario cannot fake is a process that can really be SIGKILLed. Same seam, `child_process` behind it; every other part — Agora, git, hook socket, `AgentManager`, `LedgerEndpoint`, `Library`, avatar clock — is the shipped one.
- 2026-08-27 · (M4.1) The fake engine gains an `append-memory` step and honours `--resume <id>` · a recall test is only worth something if an *agent* wrote the memory, in its own directory, as prose with no schema (ADR-0006); and honouring `--resume` makes "the resumed process reports the same session id" an observable fact rather than an argv assertion.

- 2026-08-27 · (M4.2) Recall answers on the **hook socket**, at `POST /recall` · it needs exactly what that server already owns — the one 0600 socket file and the per-spawn token registry (ENGINEERING-STANDARDS §5). A second socket would be a second thing to secure, a second stale file to clean after a crash, and no capability the first did not already have. The plan's phrase for this is "the eph-hook single-client discipline".
- 2026-08-27 · (M4.2) `eph-recall` does **not** fail open, unlike `eph-hook` · a hook that cannot be delivered costs the agent nothing, so the hook shim swallows it. A recall that printed nothing when the harness was down would have the agent conclude the company knows nothing and act on it — the silent fallback invariant §7 names as the one unforgivable mode. Every failure prints `recall unavailable: <reason>` and exits 1, and PROTOCOL.md tells agents the difference between that and "nothing matched".
- 2026-08-27 · (M4.2) The keyword index is `index/fts.sqlite`, **not** a table in `db.sqlite` · SDD §10 repairs a corrupt recall index by deleting and rebuilding it, and `db.sqlite` holds the append-only cost ledger. Derived state a repair may delete does not share a file with durable state that must never be deleted.
- 2026-08-27 · (M4.2) The FTS rung is split at an `FtsStore` seam · everything that decides behaviour (the mtime gate, passage splitting, scope filtering, how a row becomes a hit) is ordinary TypeScript in `library-fts.ts`; only storage is SQLite, in a module `index.ts` alone imports. Same split as `CostLedger`/`LedgerStore` and the same honest limit: the seam cannot prove SQLite's own behaviour, so the real FTS5 path is proven by live run (recorded in PROGRESS) and the ladder — the Architect's named tested surface — is proven by the suite.
- 2026-08-27 · (M4.2) `MemoryFtsStore` scores with the same `scorePassage` the grep rung uses · so "the same known-answer query, green at every available rung" holds by construction rather than by coincidence. `recallPassages` is exported and shared for the same reason: two rungs that disagreed about what a passage is would be answering different questions.
- 2026-08-27 · (M4.2) The mtime gate compares mtime **and** size · an edit that preserves the timestamp is rare, a truncation that preserves both is rarer still, and the pair costs nothing.
- 2026-08-27 · (M4.2) The grep rung ranks distinct terms above repetition (`distinct × 10 + min(occurrences, 20)`) · a passage mentioning every term once beats one repeating a single term twenty times, which is the difference between an answer and a keyword pile. No stemming and no stopword list: both are language-specific guesses, and this rung's promise is that you can predict what it will find.
- 2026-08-27 · (M4.2) `EPH_RECALL` is composed in `AgentManager`, not in each adapter's deps · unlike `hookShimPath` it is identical for every engine, so adapters only forward it, and an adapter that forgets is caught by a conformance case. An empty command is *omitted* rather than exported empty — `$EPH_RECALL q` would otherwise run `q`.
- 2026-08-27 · (M4.2) FTS5 terms are quoted and OR-ed · a query containing FTS5 syntax (`AND`, `*`, `"`) must be searched for, not executed; OR with bm25 ranking gives the same "more distinct terms ranks higher" shape the grep rung scores by.

- 2026-08-27 · (M4.3) The driver is written against MemPalace **3.8.0's real CLI**, verified by running it · `--palace` is a global flag that must precede the subcommand; `mine <dir> --wing <w> --agent <a>`; `search <q> [--wing w] [--results n]`. `search` has no machine-readable mode, so the human output is parsed — tolerantly, and an unrecognized shape returns null (degrade the rung) rather than an empty list (answer "nothing known"). That distinction is the whole point: the two are different facts.
- 2026-08-27 · (M4.3) `MEMPALACE_HOOKS_AUTO_SAVE=0` and `MEMPALACE_HOOKS_DAEMON=0` are set on **every** invocation, not once in a config file · ADR-0016's consequence is that the archive has one writer path, and a rule that lives in a file the Architect may edit is a rule with no mechanical backing — the class this project refuses elsewhere. `--daemon`/`--background` are never passed and `mempalace hook` is never installed into any engine's settings; three tests hold all of it.
- 2026-08-27 · (M4.3) `RecallIndex.sync`/`search` became **async** · a rung may now be a local subprocess, and a semantic search takes seconds. Doing it synchronously would freeze the Electron main process and with it every agent's terminal. `Library.recall`/`reindex` follow; the `/recall` route already awaited.
- 2026-08-27 · (M4.3) The driver keeps its **own** mtime gate on top of MemPalace's file-level dedup · MemPalace skips already-filed files itself, but a `mine` still costs a process start and an embedding pass, so a wing whose documents are all unchanged is not invoked at all. Proven by counting invocations of the fake CLI, not by trusting the report.
- 2026-08-27 · (M4.3) A hit's `ref` is rebuilt from the wing's directory · MemPalace reports `Source: memory.md`, a basename; the driver knows which directory it mined into each wing, so the Architect gets a path they can open instead of a name they cannot resolve.
- 2026-08-27 · (M4.3) `config.json` grows an **optional** `mempalaceCommand` · MemPalace is commonly installed into a virtualenv or with pipx, and "must be on PATH as `mempalace`" would make the top rung unreachable for a normal install. Optional, so an existing config stays valid with no migration.
- 2026-08-27 · (M4.3, found by the live run) `snippetOf` now opens the window where the most **distinct** terms are, not at the first match · in "why is checkout unreliable", `is` matches in the second line of every document, so every snippet opened on boilerplate while the sentence that answered sat outside the window. Deterministic, earliest-wins on ties; it improves every rung.
- 2026-08-27 · (M4.3, recorded limit) MemPalace files whole documents as drawers · so the top rung's granularity is a file where the keyword rungs return a passage, and a memory drawer carries the seed preamble with it. The snippet is bounded and windowed, which contains the effect; changing what is mined would mean not writing the preamble agents read, which is worse.
- 2026-08-27 · (M4.3) ADR-0016 §2's "one wing per target/project" is **not** implemented · the wings here are one per agent plus one for the knowledge shelf, all Agora content. Target repos are the agents' working directories and nothing in M4 mines them. Recorded so the gap is visible rather than looking like an oversight.

- 2026-08-27 · (M4.4) Reflection asks the agent; the harness never summarizes · ADR-0005 rejects "the harness calls a model API directly" outright, and condensing a memory is judgement, not mechanism. The job notices a memory is over threshold, delivers a harness prompt as a normal `request`, and applies whatever the agent proposes back to `agent.library`. The module has no opinion about what a good condensation says.
- 2026-08-27 · (M4.4) `agent.library` is the reflection endpoint, on the ratified reserved-id rule · same standing rule the Architect ratified for `agent.ledger` at the M3 close, so SDD §4.4's `to` domain is untouched. Unlike the ledger it has **no writer check**: any agent may write there, because the endpoint condenses `message.from`'s memory and nobody else's — an agent writing to it can only ever act on itself.
- 2026-08-27 · (M4.4) `Library.condense()` is the one method allowed to rewrite `memory.md` · everything else appends. It is allowed only because nothing is lost: the archive is written FIRST (a crash between the two writes duplicates a section, which is recoverable; the other order loses one), and `nothingDestroyed` verifies every old section survives byte-for-byte in the new memory or the archive before the rewrite is committed. A failed check writes nothing and throws.
- 2026-08-27 · (M4.4) `nothingDestroyed` does **not** count the condensed core as containing what it summarizes · a summary is not the thing it summarizes, and treating it as one is exactly how "nothing is destroyed" quietly stops being true.
- 2026-08-27 · (M4.4) Reflection needs **no new `log.jsonl` kind** · SDD §4.3's kind list is closed and adding one would be a §8 deviation. It does not need to: the request, the agent's proposal and the endpoint's answer are all *messages*, so `Hermes.deliverFromHarness` (new, and now also used by the ledger endpoint's replies) logs the same `delivery` entries the router logs. The trail — who was asked, what they answered, how many sections went to which archive file — is reconstructible from `log.jsonl` alone (NFR-13). The accept subject carries the count and the file name precisely so the log entry does.
- 2026-08-27 · (M4.4) Threshold 24 000 chars, keep the newest 5 sections · three times the injection budget: reflection should fire because memory has genuinely outgrown a session's context, not the moment the budget first elides anything. A long memory in few sections declines *visibly* — one enormous entry cannot be condensed by moving sections, and archiving the whole file would leave nothing behind.
- 2026-08-27 · (M4.4) An unanswered request is re-asked after 6 h, and an agent that leaves is reported · deferred, visibly, never dropped. Asking twice is harmless: the endpoint applies whichever answer arrives against the plan as it stands then, and refuses one for a memory no longer due.
- 2026-08-27 · (M4.4) The scheduler stamps a trigger's clock **before** the run · a job that takes longer than its interval would otherwise be instantly due again on the tick it finished. With re-entrancy already blocked, this is what keeps a slow job on its interval instead of back-to-back.

- 2026-08-27 · (M4.5) Four IPC channels join the surface, with their SDD §5 line in the same commit · `agora:memory` is SDD §5's documented `memory(id)`; `agora:recall`, `agora:knowledge` and `agora:register-knowledge` widen it. The M3 close-out set the rule for exactly this (`watch:dismiss` "had widened the documented IPC surface without the doc line + log entry… by the repo's own M3.1 rule that pair is mandatory"), so both are here. Surfaced in the session report so the Architect can object.
- 2026-08-27 · (M4.5) A shelf document's name is checked **twice** — in the IPC schema and again in `library.knowledgePath` · the renderer is untrusted (invariant §2), and a traversal that only the schema refused would be one refactor away from being possible again. The Library's check is the one that guards the write.
- 2026-08-27 · (M4.5) The Library writes the shelf file; `index.ts` commits it · ADR-0004 gives the Agora exactly one committer, and `library.ts` runs no git. The commit is queued (`commitSoon`), never awaited — the panel must not wait on git.
- 2026-08-27 · (M4.5) The rung chip is coloured by rung · `mempalace` in status-success, `fts` in status-working, `grep` in status-looping. A chip that looked the same on every rung would be a degradation nobody notices, which is the failure invariant §7 names.
- 2026-08-27 · (M4.5, environment) The Memory panel's **screenshot is owed to a local session** · `electron-rebuild` cannot complete here: this environment's proxy answers 403 to `www.electronjs.org`, so node-gyp cannot fetch Electron headers, `better-sqlite3` stays Node-ABI and `npm run dev` cannot boot. Everything provable without Electron was proven by live run of the shipped IPC deps; the pixels are recorded as owed, never faked — the same discipline the Architect's MemPalace directive prescribes.

- 2026-08-27 · (M4.6) The codex adapter was written against a **real `codex-cli 0.150.1`**, installed and run in this session · every flag it uses (`--cd`, the positional `[PROMPT]`), its version line (`codex-cli 0.150.1`) and the existence of `--dangerously-bypass-hook-trust` were checked by running the binary, not inferred from how another engine works.
- 2026-08-27 · (M4.6) Codex ships at **`pty-heuristic`**, writing no settings at all · it has a hook plane, but Codex refuses to run hooks without persisted trust and the only override is `--dangerously-bypass-hook-trust` — the harness lowering a permission default on the Architect's behalf is exactly what the Watch exists to prevent and a §8 must-ask, not an implementation detail. The hook file's schema also could not be confirmed against the real CLI. Declared = demonstrated, so: no events claimed, nothing written into anyone's repo, and the card says `pty-heuristic`. Raising the grade owes a live demonstration first.
- 2026-08-27 · (M4.6) Identity reaches codex as the **first prompt** · SDD §3 names it as one of the three sanctioned mechanisms, and it is the only one available: codex has no `--append-system-prompt`, and its context file (`AGENTS.md`) is not a local/gitignored variant, which ADR-0009 forbids writing.
- 2026-08-27 · (M4.6) Codex declares **no transcript reader** · it records sessions under `$CODEX_HOME`, but this environment has no codex credentials, so the on-disk format is unverified. A reader written against a guessed format would fold invented numbers into the append-only cost ledger — the one thing ADR-0011 must never do. Spend for a codex agent reports source `none`, visibly.
- 2026-08-27 · (M4.6) The conformance table's two *installer* hygiene cases are now conditional on the adapter declaring settings · an adapter may legitimately install nothing, and the table could not express that tier — it crashed on `settingsRel[0]` being undefined. A new assertion holds the other half: an adapter that declares no settings must actually write none. This is the containment wall growing to fit a grade ADR-0009 already defines, not a core change.

- 2026-08-27 · (M4.7) The gemini adapter was written against a **real `gemini` 0.57.0** and its own bundled hook docs · the version line, the positional `[query..]`, `--skip-trust`, `--resume`'s argument domain and the `gemini hooks` subcommand were all read off the running CLI, not inferred.
- 2026-08-27 · (M4.7) Gemini ships at **`pty-heuristic`**, for a different documented reason than codex's · its hook plane is the best documented in the roster after Claude Code's, but its project settings file `.gemini/settings.json` is **tracked** (its own docs `git add` it) and it has no project-local variant — ADR-0009 permits writing only local/gitignored variants — and project hooks are untrusted by default with `--skip-trust` as the sole override. Both are §8 must-asks. The wiring is a small change away once the Architect rules; the grade moves only with a live demonstration.
- 2026-08-27 · (M4.7) Gemini declares **no `resume`** even though `-r/--resume` is an appendable flag · its argument domain is `"latest"` or an index, never a session id, and `ResumeSupport.resumeArgs` is handed the id the event plane recorded. `--resume latest` would reopen whatever ran most recently in that directory — for two agents sharing a repo, exactly the cross-attribution bug M3 spent a package removing from the cost ledger.
- 2026-08-27 · (M4.7) `-p/--prompt` is never used · it runs gemini headless and exits, and an agent whose terminal is not interactive cannot be steered (FR-1.3). A test pins it.

- 2026-08-27 · (M4.8) Worktree management lives in `git.ts` · ADR-0004 gives the app exactly one git path and "only main runs git" is only checkable while every invocation is in that file. `AgentManager` gets a four-method seam (`pathFor`/`branchFor`/`create`/`remove`) and cannot become the place a second git path grows; the invariant tripwire still passes unchanged.
- 2026-08-27 · (M4.8) `Worktrees` refuses the Agora itself **and anything under it** · the guard is a resolved-path prefix check, not a string compare, and it is enforced in the class rather than at the call site — a rule the caller could forget is not a rule.
- 2026-08-27 · (M4.8) `--force` never appears · a worktree with uncommitted work is kept, the porcelain lines come back as evidence, and the refusal is *reported* through `onExitError`, not merely logged. An agent that died with unpushed work leaves that work for the Architect; tidiness does not outrank somebody's afternoon.
- 2026-08-27 · (M4.8) An unreadable worktree counts as **not clean** · refusing to remove something we cannot inspect is the safe direction.
- 2026-08-27 · (M4.8) `create` reuses an existing branch instead of failing on it · a respawn must land where the dead process left off, and `branchFor` is stable per agent. The card records `branchCreated` so a reader can tell the two cases apart.
- 2026-08-27 · (M4.8) The cwd is swapped **before** the version probe and before any settings are written · everything downstream reads the card's `cwd`, so grants, the settings install and the transcript directory all follow the worktree with no further plumbing. `LiveAgent` keeps `targetRepo` because removing a worktree is an operation on its repo, and by then the card no longer points at one.
- 2026-08-27 · (M4.8) A worktree that cannot be made is reported and the spawn continues in the target repo · isolation is a nicety; refusing to hire over it would be worse than saying so out loud.
- 2026-08-27 · (Architect verdict, M4 close) Codex stays `pty-heuristic` · the only path to codex hooks is `--dangerously-bypass-hook-trust`, and the harness lowering a trust default on the Architect's behalf is what the Watch exists to prevent. Hook wiring is owed to a local session where the Architect persists trust interactively and the hook schema can be confirmed against the real CLI.
- 2026-08-27 · (Architect verdict, M4 close) Gemini stays `pty-heuristic` · its hook wiring lives in a TRACKED settings file, and ADR-0009 permits adapters to write only local/gitignored variants — an adapter must never dirty a repo's tracked files. Revisit only if gemini ships an untracked variant.
- 2026-08-27 · (Architect verdict, M4 close) No-resume stands for codex and gemini · ADR-0009's append-only `ResumeSupport` cannot express a subcommand-first resume or an index-based one that would reopen whoever ran last (the cross-attribution class M3 removed). The plan-transform generalization is a recorded candidate for a future ADR-0009 annex — not to be invented mid-package.
- 2026-08-27 · (Architect verdict, M4 close) SDD §4.3 gains the `memory` log kind · added on the Architect's call so the Library's future events have a home; nothing emits it yet, and its first emitter documents its refs.
- 2026-08-27 · (Architect verdict, M4 close) The four Library IPC channels are RATIFIED after channel-by-channel review · `agora:memory`/`agora:recall`/`agora:knowledge` (reads) and `agora:register-knowledge` (name-sanitized, committer-routed write), all zod-validated in main.
- 2026-08-27 · (M4 close-out audit) The reflect-request subject and the library-unavailable reply render from `prompts/library/` · three LLM-facing literals had survived in main (invariant §8); Hermes's tests-only fallback went mechanical to match its own render() standard.
- 2026-08-27 · (M4 close-out audit) The recall smoke's reporting case is falsifiable both ways · `EPH_MEMPALACE` set-but-broken now FAILS the suite (a configured-but-dead rung is the invisible degradation invariant §7 forbids); unset asserts the absence. The old assertion was a tautology that could never fail.
- 2026-08-27 · (M4 close-out audit) A surviving worktree on the agent's own branch is REUSED at respawn · the dirty-unwind keep and the respawn's "already exists" refusal contradicted each other — the card named a worktree the spawn refused to enter. Anything at the path that is not the agent's own checkout still refuses. Two regression tests.
- 2026-08-27 · (M4 close-out audit) `postJson` in `shims/hook-client.mjs` is the ONE low-level socket transport; `eph-recall` delegates to it · the recall shim had grown a duplicate `http.request` implementation against M1.2's single-client rule. Hooks map the shared transport fail-open, recall maps it fail-closed — the contracts differ, the transport does not.
- 2026-08-27 · (M4 close-out audit, recorded not fixed) The codex commit also adjusted the shared conformance harness (+50/−18, disclosed in-commit) to admit a no-settings adapter · TEST-STRATEGY §5's "only its adapter" reading is noted; the change was necessary and reviewed. Also recorded: the declared-vs-demonstrated live check runs only against the fake engine, so codex/gemini honesty reduces to "declares pty-heuristic + provably writes nothing"; and the exit table's `attribution ok (59 commits)` was produced on pre-M4 main — corrected in place, the record-hygiene class the M3 audit named.

- 2026-08-28 · (M5.1) The agent↔task binding is **derived from `tasks.json`, never remembered** · an in-memory agent→task map would be empty after exactly the event that makes the binding matter most — a restart — and the ledger already records who owes what durably. `boundTaskFor` reads the book of record instead of keeping a second copy that could disagree with it, which is the reasoning invariant §11 applies to spend.
- 2026-08-28 · (M5.1) The binding ranks `in_progress` over `todo`, then lower priority, then id · status must outrank priority or a gate would attach to work the agent has not started; the id tie-break makes the answer STABLE under file reordering, because an unstable binding would open a gate against one task and settle it against another. Asserted by a table that reorders the same two tasks.
- 2026-08-28 · (M5.1) The breaker report goes to `log.jsonl`, not into the task · ADR-0011 says the task returns "with the breaker report attached", but `taskSchema` is strict and has no notes field, and widening a normative schema would be a §8 deviation. `stallTask` moves the status and the `task`/`stalled` event carries `because`, `rung`, `signals` and `detail` — the same split `returnTasksOf` already makes for SDD §10's crash note, and what NFR-13 asks for.
- 2026-08-28 · (M5.1) `BreakerEffects.returnTask` is **required, not optional** · rung 3 always owes the clause, and an optional effect would let a construction site silently drop it — exactly how the clause went unimplemented from M3 to M5. Four construction sites were updated rather than defaulted.
- 2026-08-28 · (M5.1) `test/scenarios/company.ts` now runs the SHIPPED `LedgerEndpoint` and wires `taskOf`/`returnTask` · the rig already carried the "SHIPPED choke-point wiring, not a copy of it" rule after review found a duplicated `index.ts`; the join gets the same treatment, so the scenarios exercise it rather than describing it.
- 2026-08-28 · (M5.1, environment) `core.autocrlf` is set **false** in this clone · with it true the fast-forward materialized every touched file with CRLF, and `prettier --check` (default `endOfLine: lf`) then failed on 135 files — a red baseline caused entirely by local git config, with CI green on the same commit. The working tree was re-materialized with `git checkout-index -a -f`; zero content changed. A repo-level `.gitattributes` with `* text=auto eol=lf` would prevent the next Windows clone from hitting it — recorded as a candidate, not taken, because it is outside this package.
- 2026-08-28 · (M5.1, recorded not fixed) `src/shared/breaker.ts` contains two **literal NUL bytes** — the span key separator is written as a raw `\u0000` character rather than the escape — so git classifies the file as binary: `git diff` shows only `Bin 9634 -> 9944 bytes`, and `grep` skips it without `-a`. A Watch-critical file is therefore unreviewable by diff. Pre-existing (present in `HEAD` before this package) and out of M5.1's scope; the fix is a one-character-class change with identical semantics. Flagged for the Architect.

- 2026-08-28 · (GYM-002) Rung-1 steer rides the **existing decision channel on `post-tool`**, not `additionalContext` · the proposal named the upstream mechanism; the shim already relays `{decision, reason}` verbatim for every event and the engine's documented post-tool semantics for `block` are "prompt the model with the reason" — the same hook-boundary latency with zero shim/adapter changes, where `additionalContext` would have needed engine-specific event names pushed into shim args. Mechanism recorded as a deviation inside the approved scope; the merge-risk the proposal flagged vanishes with it (steer and Stop answer different events).
- 2026-08-28 · (GYM-002) One pending note per agent, latest wins, delivered exactly once, cleared on `session-start` · a second trip's sentence must not queue behind a stale one, and a successor session must never be steered by a sentence aimed at its dead predecessor. All four rules unit-tested.
- 2026-08-28 · (GYM-002) Channel visibility is a `breaker`/`steer-channel` log event rather than a new agent-card field · the card already shows the hook grade, which *is* the channel selector; the log carries which channel each sentence actually took (NFR-13). The card field can ride a later UI pass if the Architect wants it at a glance.
- 2026-08-28 · (GYM-002, environment) `test/main/agent-worktree.test.ts` (4) and `test/scenarios/s-crash.test.ts` (3) fail **at HEAD** in this worktree — spawn-phase timeouts ("timed out waiting for the agent to start", card stuck `installing`) in the AgentManager probe path, verified pre-existing by running both suites with the GYM-002 diff stashed. Not touched by this package; CI (ubuntu) is the gate that counts. Flagged for the Architect: likely this machine's state (OneDrive worktree path), worth a look before the next milestone close.

- 2026-08-28 · (GYM-003) Acks travel as an **endpoint hand-off to `agent.closing`**, a fourth reserved id · the alternative was watching inbox traffic or magic subjects on ordinary mail; the M3 standing rule for harness-owned correspondents already exists and the routing table already knows how to guard an endpoint (`inform`/`done` only — an ACK never asks). `reserved.ts` refuses the id at spawn, so no hire can forge a closing acknowledgment.
- 2026-08-28 · (GYM-003) The harness, not Artemis, runs the protocol · upstream mails its orchestrator a shutdown brief and lets it broadcast; here Artemis may be among the workers being closed, and a protocol that depends on one agent's liveness to save every other agent's memory has its dependency backwards. `ClosingTime` mails every live agent directly via `deliverFromHarness` and watches the endpoint.
- 2026-08-28 · (GYM-003) An ack is `in_reply_to` the request **or** the exact `CLOSING-TIME-ACK` subject · engines routinely compose fresh messages instead of threading replies; either signal is unforgeable enough (the reply routes through the sender's own outbox, which Hermes already authenticates against the directory owner). A reply that is neither is consumed and logged `closing-unrecognized`, never counted — and an ack with no closing in flight bounces with its reason (FR-3.4).
- 2026-08-28 · (GYM-003) The deadline lives in `ClosingTime`, defaulting 90 s, and the dialog is `showMessageBoxSync` in main · closing time may slow a quit, never hang it; the dialog is UI chrome (invariant §8 governs LLM-facing prose, not window copy). The live-quit evidence run owed by the success metric is booked with the metric check (2026-09-11), not skipped.
- 2026-08-28 · (M5.2) Odeon artifacts are filed as `propose` messages to a reserved **`agent.odeon`** endpoint · SDD §2 gives `odeon/` to the harness alone, so an agent files the only way it can say anything — from its own outbox — and the harness archives. That single rule is what makes the archive trustworthy: a deck cannot be back-dated, edited in place, or written for a task its author was never given. Same shape as `agent.ledger` and `agent.library`, the standing pattern the Architect ratified at the M3 close.
- 2026-08-28 · (M5.2) **The agent supplies deck CONTENT; the harness applies the TEMPLATE** · FR-7.2 requires "an HTML slide deck from the standard template", and the only way "from the standard template" is enforced rather than hoped for is for the harness to be the one that applies it. It also keeps the template in `prompts/odeon/deck.html` where invariant §8 puts every other word, and keeps `odeon/` harness-written. The agent files the six named sections as structured JSON; a missing or empty section is refused, because a deck that quietly omitted "trade-offs" would satisfy the close gate while defeating the reason the gate exists.
- 2026-08-28 · (M5.2) Every section is **HTML-escaped** into the template · a deck is agent-authored text the Architect opens in a webview, so a failing test's output containing `<script>` must render, not execute. The template is the only HTML; everything filled into it is text.
- 2026-08-28 · (M5.2) Any agent may file at the Odeon, but only for **its own assigned task, and only when that task carries the obligation** · the router stays on transport rules (ADR-0003) so the authorship check lives in the endpoint, which reads the ledger. Filing for a task that never asked for a deck is refused, or an agent could manufacture an artifact the close guard then treats as an obligation met.
- 2026-08-28 · (M5.2) The archive is append-only by NAMING, not by permission · `odeon/decks/<taskId>-<ts>.html` gives a revised deck its own file, so the version the Architect already read is never destroyed (invariant §5). `artifacts.deck` names the current one; the older ones stay on the shelf. Nothing in `Odeon` ever opens an existing artifact for writing, and a name collision is refused rather than overwritten.
- 2026-08-28 · (M5.2) `odeon:deck(ref)` is a channel SDD §5's abridged list does not name · the viewer must read one artifact's HTML, and `decks()` returning every deck's body would make the list a copy of the archive. Added with the ref validated in main and resolved only to a well-formed deck name inside the archive directory — a renderer-supplied path never escapes it. Recorded here with SDD §5 updated to name it, the same way `watch:dismiss` was handled at the M3 close.
- 2026-08-28 · (M5.2) An Architect review comment becomes **mail to the orchestrator, never a task** · UC-05 step 4 says comments "become follow-up tasks", but FR-5.2 gives the ledger to Artemis and ADR-0005 keeps the harness out of deciding what work the company does. The comment is delivered to her as a `request` carrying the deck ref; what task it becomes is her judgment, and `prompts/odeon/deck-comment.md` tells her not to invent one to look responsive. A comment with no orchestrator to receive it reports that rather than vanishing.
- 2026-08-28 · (M5.2) The relayed comment is signed **`from: agent.odeon`**, not `from: human` · SDD §4.4 puts `human` in the ADDRESS domain only; `from` must be an agent id, and `messageSchema` enforces it. The Odeon relays and signs as itself — the same reserved-identity answer M3 gave the router's bounce, which had claimed `from: <the original sender>` for a message the sender never wrote. The template names the Architect as the author.
- 2026-08-28 · (M5.2) The deck viewer renders in a **sandboxed iframe with no `allow-scripts`** · the archive escapes agent content on the way in; refusing to execute it on the way out is the second wall, and the two are independent.
- 2026-08-28 · (M5.2, environment) eslint now ignores `.claude/**` · a `.claude/worktrees/` scratch checkout of this same repo (git-ignored via `.git/info/exclude`, created by other tooling mid-session) put a second tsconfig root under the project and made every file fail to parse — 383 errors with nothing wrong in `src/`. Prettier already ignored `.claude/`; this makes eslint agree, so the gate reports on this checkout rather than on transient agent scratch space.

- 2026-08-28 · (M5.3) The memo hold IS a Watch gate, not a second holding mechanism · SDD §7.3 draws the flow as "Watch gate policy match → action held", so a matching action opens an ordinary gate carrying `memoTrigger`, and the memo's verdict settles that gate. One hold mechanism, one queue, one release path — a parallel memo-hold would have needed its own crash recovery, its own UI and its own reconciliation.
- 2026-08-28 · (M5.3) A memo trigger borrows an existing gate KIND rather than inventing a seventh · `GATE_KINDS` is a documented closed set: new-dependency and api-or-schema-change ride `scope-change`, security-posture rides `prod-facing` (it is the kind whose blast radius the Architect reads as "this reaches the deployed system"), spend rides `spend`. The trigger itself is on the gate, so the mapping loses nothing.
- 2026-08-28 · (M5.3) The trigger table matches generously on PATHS and conservatively on TEXT · a false positive costs one memo; a false negative is a dependency that landed with nobody's signature on it. Manifest file names, contract files and security files match by name or path fragment; commands match only on explicit install verbs.
- 2026-08-28 · (M5.3) `security-posture` is checked BEFORE the contract triggers · an action can match twice (a migration that rewrites permissions), and "this also changes auth" is the more consequential framing to put in front of a reviewer.
- 2026-08-28 · (M5.3) **The harness writes the countersignature; the decider never claims it** · `verdictFilingSchema` carries only `memoId`, `verdict` and `notes` — `decidedBy`, `countersigned` and `authority` are filled from `mayDecide`, and a filing that tries to set any of them is refused as an unknown field. An orchestrator that could write its own countersignature could grant itself authority it was never given, which is the widening FR-5.5 exists to prevent. Three table-driven cases pin it.
- 2026-08-28 · (M5.3) `mayDecide` is asked AGAIN when the verdict arrives, not trusted from the triage · the verdict comes back as mail, and mail can arrive late, out of order, or from an orchestrator whose authority table changed in between. The countersignature must describe the authority that exists now.
- 2026-08-28 · (M5.3) `memoVerdictSchema` REFUSES a delegated verdict with no countersignature or no named grant · FR-5.5 is enforced by the validator rather than by the caller remembering, so no code path can write an unsigned delegated decision into the archive.
- 2026-08-28 · (M5.3) A memo gets exactly ONE verdict · `recordVerdict` refuses when `verdict.json` exists. An approval that could be quietly replaced after the action it released had already run would make the archive a claim rather than a record (invariant §5).
- 2026-08-28 · (M5.3) `amended` denies the gate, like `rejected` · an amendment is a *different* action from the one that was held, so the agent must come back with the amended plan rather than have the original released under a verdict that was not about it.
- 2026-08-28 · (M5.3) A memo's header is read back out of `memo.md`, not kept in a third file · SDD §4.5 gives a memo directory exactly two files, so the facts a verdict needs (gate, trigger, task) round-trip through the markdown this module also writes. `parseMemoHeader` is tested both ways.
- 2026-08-28 · (M5.3) `odeon:verdict` carries no `decidedBy` · a verdict arriving through the window bridge IS the Architect and main knows that with certainty. Taking the renderer's word for who decided would let an untrusted surface stamp a countersignature onto the permanent record of a delegated decision — the same reasoning that keeps a channel off `watch:approve` (invariant §2).

- 2026-08-28 · (M5.4) The briefing runs one way only: **the harness compiles facts, Artemis narrates them** · ADR-0005 keeps judgement out of the harness, so `compileFacts` has no prose and no opinions, and the narration comes back as mail. The direction is enforceable in exactly one place — every sentence must carry a ref that resolves to a fact the compiler issued — which is what turns FR-7.1's "never from free recollection" from an instruction into a mechanism.
- 2026-08-28 · (M5.4) An **unresolvable ref refuses the whole brief**, and nothing is archived · an invented citation is worse than none, because it looks checked. `checkNarrative` names every unsupported sentence at once so one round-trip fixes them all, and the refused brief leaves no artifact behind.
- 2026-08-28 · (M5.4) The spoken-length budget is **word-count math**, not a synthesis round-trip · SRS §6.2 measures ≤ 90 s at VOICE-DESIGN's 150 wpm, and the check has to work before the Herald exists (M6) and without spending a TTS call to discover a brief is too long. A test raises the budget and re-passes the same text, so the check is provably about length and not about the words.
- 2026-08-28 · (M5.4) The headline is chosen **mechanically by consequence**, not by importance · gates before memos before stalled work before completions. "Which is most important" is judgement and belongs to Artemis; "which of these must the Architect act on" is a fact, and the compiler may know it.
- 2026-08-28 · (M5.4) A quiet window still produces a headline fact saying so · a brief that vanished because nothing happened is indistinguishable from a brief that failed (invariant §7).
- 2026-08-28 · (M5.4) The outstanding fact set lives **in memory**, deliberately · it is a question in flight, not a record. A restart that loses it costs one skipped standup; persisting it would risk checking a narration against a window that had already moved on. The archived brief on disk is the record, written only once the answer passes.
- 2026-08-28 · (M5.4, defect found by the live run) A refused brief must NOT settle the outstanding ask · `archiveBrief` settled unconditionally, so a refusal closed the question and the corrected narration was then rejected as answering a brief nobody had asked for — the refusal was terminal and the retry impossible, which defeats the point of returning reasons. The rule moved out of the wiring and into `BriefingJob.narrated(briefId, accepted)` so it is asserted at the module boundary, with two regression tests.
- 2026-08-28 · (M5.4) Cumulative spend for a brief is folded from the durable ledger per agent · `totalOfSpend` calls `spendFor(agentId, null).cumulativeTotals`, never a counter this process kept (ADR-0011, invariant §11).

- 2026-08-28 · (M5.5) The driver owns **who may speak**, and nothing else · Artemis chairs — what to ask, of whom, and when the meeting is done stays hers (ADR-0005). The driver's whole contribution is that two people can never hold the floor at once and that nobody's answer is discarded for arriving early.
- 2026-08-28 · (M5.5) An out-of-turn reply is **held and later released**, in ATTENDEE order · holding is only worth anything if it pays out, so accepting a turn drains every held reply the floor has now reached. Attendee order rather than arrival order, so the transcript reads the way the meeting was convened rather than the way the network delivered it.
- 2026-08-28 · (M5.5, defect found by a test) The floor is handed out after EVERY accepted turn, never only when the holder changed · a drain that releases held replies can wrap the floor back to the same agent, and comparing floor identity there left nobody asked — a full transcript and a stalled meeting. The guard was a premature optimisation that introduced a stall.
- 2026-08-28 · (M5.5) An Architect interjection is recorded immediately and re-asks the holder · the Architect is not a speaker waiting a turn, they are the reason the meeting is happening. Naming an attendee hands them the floor (and releases anything they had said early); naming nobody leaves the floor put, so an aside does not cost the current speaker their turn.
- 2026-08-28 · (M5.5) A non-attendee is REFUSED, not held · holding implies a turn is coming, and somebody outside the meeting has none.
- 2026-08-28 · (M5.5) The minutes print what **never reached the floor** under its own heading · a meeting closed while somebody was still waiting to speak is a fact about that meeting; hiding it would make the minutes a summary instead of a record.
- 2026-08-28 · (M5.5) Action items go to the ORCHESTRATOR, not to `tasks.json` · FR-4.2 gives the ledger one scribe, and a driver that wrote tasks would be the second writer ADR-0004 spent a package removing. The minutes are the harness's to write (SDD §2 gives it `odeon/`); the tasks are hers to propose. Proven live: a closed meeting left 0 tasks and 1 message to the orchestrator.
- 2026-08-28 · (M5.5) The Odeon endpoint now accepts `inform` as well as `propose` · the floor is handed out as a `query`, and ADR-0003's act table makes the reply to a query an `inform` — refusing it would leave the driver unable to hear its own attendees. `propose` still means "file an artifact", and a test pins that an `inform` carrying a deck-shaped body never becomes a deck.
- 2026-08-28 · (M5.5) Attendees gather via the EXISTING station map · SDD §6 already routes the `meeting` tool class to the `odeon` station, so attendance is `pre-tool`/`post-tool` on the avatar director and needs no new avatar state.
- 2026-08-28 · (M5.5) One meeting at a time · two live meetings would put the same agent on two floors, and the Odeon room on the floor plan is one room. A second convene is refused rather than replacing the first, which would lose a transcript nobody had archived yet.

- 2026-08-28 · (M5.6) Every org figure is **folded from the book of record on every read** · invariant §11's rule applied a second time. There is no counter anywhere in `shared/org.ts`: tasks done, rework, escalations and tokens are all counts over `log.jsonl` plus the durable cost fold, recomputed per call. A counter is a second record a restart silently zeroes, and a metric nobody can recompute is a metric nobody can argue with.
- 2026-08-28 · (M5.6) An agent that did nothing appears with **zeroes, not omitted** · "did nothing this week" and "is not in the company" are different facts, and a report that could not tell them apart would be the wrong kind of quiet.
- 2026-08-28 · (M5.6) Rates are **null, not zero**, when nothing has completed · dividing by no tasks is undefined, and reporting 0 would say "perfectly efficient" about an agent that has finished nothing. The renderer prints `—`.
- 2026-08-28 · (M5.6) An event is attributed by whichever key names its agent — `agentId`, `assignee`, `by` or `from` · the log's kinds carry the agent differently, and reading only one would quietly under-count exactly the kinds that matter most (gates and memo escalations).
- 2026-08-28 · (M5.6) The org chart is **derived from the roster, never stored** · the roster IS the org model, and a second copy would be free to disagree with who is actually hired. Ordering is orchestrator-first then role then id, so two runs of the same report never differ by shuffling.
- 2026-08-28 · (M5.6) Every retro finding carries refs, exactly like a brief's sentences · a claim about somebody's week that they cannot check is an opinion with a number next to it.
- 2026-08-28 · (M5.6) **The org layer computes and archives; it never acts** · no reassignment, no hire, no nudge. UC-12 puts a human between the numbers and any decision, and the archived report says so in its own last section — a report that read like a verdict would invite the company to act on it without one.
- 2026-08-28 · (M5.6) A retro is written once and never revised · a second generate at the same name is refused rather than overwriting (invariant §5), and retros archive beside the other Odeon artifacts because they are the same kind of thing.
- 2026-08-28 · (M5.6) Hire templates carry env grant NAMES and a version · `<name>@<version>` is how a registry row refers back to the file it was hired from; a template carrying a secret VALUE would be the leak ADR-0010 exists to prevent, and the strict schema refuses the extra field.

- 2026-08-28 · (M5.7) **Authority-widening is refused as a property of the PROPOSAL, not of any verdict** · `checkWidening` takes no decider, so there is no argument an approver could pass to make a widening proposal acceptable — the Architect included. FR-12.3 does not say "unless the Architect says otherwise". An Architect who genuinely wants to change an invariant edits the document and supersedes the ADR by hand, outside the loop and in the open, where it is reviewable. A door that only opens from the inside is not a door.
- 2026-08-28 · (M5.7) The widening matcher reads the title, the change AND the rollback · a proposal that hides the widening in its rollback is the same proposal. Matching is generous: a false positive costs one rewrite, a false negative is the company quietly editing the rules that govern it.
- 2026-08-28 · (M5.7) `GymDecider` has exactly one inhabitant — `'architect'` · R1 is not a runtime check bolted onto a general "decider": the only decider the Gymnasium has a word for is the Architect, so a caller trying to express "Artemis approved it" has nothing to write down. The redundant proposer-is-decider check stays anyway, because the day somebody adds a second decider is the day it stops being redundant and nobody will remember to add it.
- 2026-08-28 · (M5.7) `gym:verdict` carries **no decider field at all** · main supplies `architect` in the handler because a call arriving on the window bridge IS the Architect. Giving the renderer a field to name a decider would hand an untrusted surface the one authority ADR-0015 reserves — the same reasoning that keeps a channel off `watch:approve`.
- 2026-08-28 · (M5.7) A proposal is refused **before any human sees it**, with every reason at once · FR-12.2 puts the harness first, so the proposer's only feedback is that list, and a list of one wastes a round trip per missing field.
- 2026-08-28 · (M5.7) An **unmeasurable** metric counts as a regression, not a pass (FR-12.4) · a change whose effect cannot be established is not a change that worked, and the proposal's rollback is what happens next. The log event carries `rollback: true` so the obligation is findable.
- 2026-08-28 · (M5.7) The ledger is append-only in the sense ADR-0015 means · rows advance in place along the documented flow, and `append` throws if a write would ever shrink the row count. Rejected and regressed rows stay, because R2 calls them training data for better proposals — a ledger that hid its failures would be a highlight reel.
- 2026-08-28 · (M5.7) The ledger **seeds from the repo's own `docs/gymnasium/`** at first run (FR-12.6) · the improvement archive is continuous from the build phase into the running system rather than starting empty on the day the product ships. A missing seed degrades visibly and starts empty; it never fails the boot.
- 2026-08-28 · (M5.7) Gym proposals are filed at the SAME `agent.odeon` endpoint as the other artifacts · SDD §7.6 makes a proposal an Odeon memo subtype, and ADR-0015 rejects the separate meta-agent outright: a second governance system is a second place for governance to fail.
- 2026-08-28 · (M5.7) `src/shared/gym-view.ts` holds the renderer-facing types · `shared/ipc.ts` and the sandboxed preload must stay free of runtime dependencies (BUILD-PROMPT §10.4), and `gym.ts` imports zod.

- 2026-08-28 · (M5.8) The Odeon endpoint dispatch was extracted to `src/main/odeon-endpoint.ts` · one address takes five filings, and both `index.ts` and the scenario rig now call the SAME factory. This is the M2 close-out lesson applied before it could bite again: the gate choke points had been wired inline in `index.ts` and copied character-for-character into the rig, so a scenario stayed green with the production wiring deleted.
- 2026-08-28 · (M5.8) The scenario rig feeds `submitMemoTrigger` off the same `pre-tool` stream `index.ts` uses · without it S-MEMO would have asserted a hold the running app never opens. Found by writing the suite: the first draft passed the hold test and failed everything downstream, because no gate existed.
- 2026-08-28 · (M5.8) The rig's org layer reads `hermes.knownAgents()` rather than the registry · a rig hire makes a mailbox rather than a roster row, so reading the registry there reports metrics for nobody.
- 2026-08-28 · (M5.8, defect found by the EXIT DEMO) The ledger endpoint's `task` log row carried no `status`, so `computeMetrics` could never count a completed task from the book of record · every org unit test passed because every one synthesised the row it wanted — the exact seam-blindness class the M3 and M4 audits named, caught this time by running the thing. The endpoint now logs the status the op left the task in, and the regression test asserts the metric against a log the REAL endpoint produced rather than a fixture.
- 2026-08-28 · (M5 exit review, gap found and closed) The standup brief had **no gym-slice section** · IMPLEMENTATION M5 names it and FR-12.5 requires the slice reported in briefings; `Gymnasium.slice()` existed but nothing put it in a brief. `compileFacts` now emits a `health` fact refd `gym:slice` naming what the slice has spent and how many proposals are open, wired in `index.ts` and the scenario rig. R3's whole point is that improvement is budgeted rather than ambient, and an ambient slice is one nobody notices growing.

- 2026-08-28 · (fix) The two literal NUL bytes in `src/shared/breaker.ts` are now written as `\u0000` escapes · the span key separator was a RAW NUL in the source, which made git classify a Watch-critical file as binary: `git diff` showed only `Bin … bytes` and `grep` skipped it without `-a`. A file nobody can read a diff of is a file nobody reviews. The separator itself is unchanged — still U+0000, still the right choice because no tool name or fingerprint can contain one — so the rewrite is byte-identical at runtime and the 82 breaker cases pass untouched. Raised at the M5 exit review as recorded-not-fixed; fixed on `fix/breaker-nul-bytes`.
- 2026-08-28 · (docs) The Decks viewer's `sandbox=""` iframe does not appear in `webContents.capturePage()` · a fully-sandboxed frame gets an opaque origin, is composited out-of-process, and Chromium's page capture misses it. The viewer is fine — a throwaway `allow-same-origin` run rendered the deck in frame and confirmed it. The sandbox was deliberately **not** loosened to make a screenshot prettier: `allow-same-origin` without `allow-scripts` is arguably harmless today, but changing a security-relevant attribute for a photograph is the wrong reason to change one. The archived deck is exported to `docs/demo/m5-deck-artifact.html` instead, which a reader can actually open.

- 2026-08-28 · (integration, defect found by the MERGE) The Gymnasium ledger parser read **zero rows out of a five-row archive** · `docs/gymnasium/LEDGER.md` writes each id as a link to its proposal file — a `[GYM-001]` label carrying a `./proposals/…` target — while the parser matched only the bare `GYM-001`. In isolation nothing failed: the M5.7 unit rig seeded from a fixture whose ids happen to be bare. The two halves only met when the branch landed beside the real archive, and the consequence was not a parse error but a silent one — a seeded ledger reads as empty, so the next mint hands out `GYM-001` to a row that is already on the page and R2's "the ledger is total" quietly stops being true. `GymRow` now carries `idCell`, the id is matched in either form, and `renderRow` writes the cell back exactly as the ledger wrote it, because the ledger is a document a human reads and not only a table a machine appends to. This is the seam-blindness class the M3 and M4 audits named, found a third way: not by a demo and not by a suite, but by two correct halves meeting.
- 2026-08-28 · (integration) The eleven M5 branches were merged to `main` **one at a time, each with `--no-ff`**, rather than squashed or fast-forwarded as a block · the parallel research-department line (closing time, the hook-boundary steer, ADRs 0017–0020) had landed on `main` underneath them, and a single collapsed merge would have made the one conflict that mattered — `test/scenarios/company.ts`, where both lines construct Hermes — indistinguishable from the trivial append-only ones in PROGRESS and the log. Each merge was gated before the next began, and the integrated tree was proven on CI (run 33177234907) on a `fix/**` branch BEFORE `main` was moved to it, so the branch that fast-forwarded `main` was a tree Linux had already run green rather than one only Windows had seen.

- 2026-08-28 · (M5 close-out audit, fixed) The Gymnasium ledger writer emitted **seven cells under the eight-column header** · `renderRow` skipped the Measured column, so `measure()` recorded an outcome that `rows()` immediately read back as null, and the next `propose()` — which rewrites every row — permanently erased every Measured cell and Outcome on the page. ADR-0015 R2 ("every measured outcome is a permanent ledger row") was mechanically false, one column to the left of where the merge-day fix had just looked. `GymRow` now carries `measured`, `renderRow` emits both cells, `measure()` stamps the check date, and the regression tests round-trip Measured/Outcome through a rewrite — including against the REAL archive's `due 2026-09-11` rows, the case finding 10 said the owed R2 tests never asserted.
- 2026-08-28 · (M5 close-out audit, fixed) The memo-verdict endpoint answers now render from `prompts/odeon/verdict-*.md` · two prose literals and a refusal subject had survived on the one Odeon path the M4-era sweep predated (invariant §8); the null-prompts fallback stays a mechanical serialization.
- 2026-08-28 · (M5 close-out audit, fixed) The Gymnasium seed copies `proposals/GYM-*.md` beside `LEDGER.md` · SDD §2 always said both; seeding only the table left every row's link broken and `proposalDoc()` null in the running system — half an archive inherited (FR-12.6).
- 2026-08-28 · (M5 close-out audit, fixed) `slice()` reports **null, not zero**, when no gym-spend source is wired, and the brief says "not yet attributed" · production wires no `gymSpend`, so the gym-slice fact was narrating a constant 0 as ledger data — the invisible-degradation class invariant §7 forbids. The *attribution* itself (tagging which spend is gym work) is genuinely deferred: it needs the task↔proposal join and belongs with the M6 scheduler work already carrying the metric-check booking. `withinSlice` treats a missing source as 0 spent, so R3's gate is honest-but-idle until then — recorded, visible in the brief.
- 2026-08-28 · (M5 close-out audit, fixed) `LOG_KINDS` gains `memory` and the reserved-id spawn-refusal test iterates `RESERVED_AGENT_IDS` · the kind was SDD-ratified at the M4 close but absent from the "verbatim" code list; the test covered two of five reserved ids, leaving GYM-003's "no hire can forge a closing acknowledgment" claim untested.
- 2026-08-28 · (M5 close-out audit, recorded) `test/scenarios/company.ts` wires Hermes without the `ledger:` endpoint option · scenarios drive `LedgerEndpoint.submit` directly, so a scenario that MAILED `agent.ledger` would get "endpoint not available" instead of the shipped dispatch. Pre-existing; owed to the next scenario that needs the mail path, not patched speculatively.
- 2026-08-28 · (M5 close-out audit, environment record completed) Three Windows/TZ test-portability defects were failing at HEAD but absent from this record · `test/shared/cost.test.ts` "splits one transcript across the days it spans" is TZ-dependent (local-time `dayKey`, fixture straddles midnight UTC only — passes under `TZ=UTC`); `test/main/engines/claude-transcripts.test.ts` "points at the engine's own project directory" fails on Windows path resolution regardless of TZ; `test/main/library.test.ts` "tells the agent out loud when the budget elided older sections" is load-flake (passes in isolation). All three predate M5 and pass on the Linux CI gate; recorded so the next Windows session reads 10 locals fails, not 7.

- 2026-08-28 · (M5b.1) Retirement is a **sibling array, not a flag on an entry** · SDD §4.7 gives the entry shape but no way to express retirement, and FR-13.1 requires `retire`. A `retiredAt` field would have made every consumer responsible for filtering it, and the day one forgets is the day the Stoa studies a source the Architect withdrew. `sources` now holds only studiable sources *by construction* and retired entries move to `retired` verbatim — the same idiom SDD §2 already uses for `inbox/` → `inbox/.done/`. Nothing is deleted, because a brief that cites a source the record no longer contains is a citation nobody can check. SDD §4.7 updated to name the array.
- 2026-08-28 · (M5b.1) `pin` is **nullable**, and null means "registered, not studiable" · §4.7 types it as a string because it describes a source that has been studied; the build-phase table carries "(set at first study)" for the rest, and FR-13.2 requires a pinned snapshot. Making the unpinned state representable is what lets `checkStudiable` refuse it — a required-string pin would have forced the seed to invent a commit, which is the one thing the provenance chain cannot survive.
- 2026-08-28 · (M5b.1) The `stoa:` IPC group is **exactly SDD §5's five channels** · no `stoa:pin`, even though `Stoa` will need to record the commit a study ran against: SDD §7.7 puts pin-setting inside the study flow, so it lands with M5b.2 that owns it rather than as an Architect button nothing yet calls. Widening a documented IPC signature is a BUILD-PROMPT §8 must-ask, not an implementation detail.
- 2026-08-28 · (M5b.1) `register` takes a **draft with no id, no registrar and no timestamp** · main mints all three, exactly as `gym:verdict` supplies its own `architect`. The strict payload schema means a renderer trying to smuggle `registeredBy: 'artemis'` is rejected at the boundary rather than quietly ignored — R1's authority is main's to assert, never the renderer's to claim.
- 2026-08-28 · (M5b.1) A damaged watchlist is read as empty and **never overwritten** · the file may be the only copy of what the Architect registered, so a boot that "repaired" it by truncating would destroy the record it could not read. The degradation is reported out loud instead (invariant §7).
- 2026-08-28 · (M5b.1) The seed copies the table **and** `briefs/` together · the Gymnasium shipped a ledger without the proposals its rows linked to and every link broke (M5 close-out audit, finding 3). A seeded source whose brief did not cross over is the same half-archive one subsystem over, so the test asserts both halves.

- 2026-08-28 · (M5b.2) An **unverified license permits study and refuses intake**; UC-14 alternate 2a's "license unverifiable" is a different state · the documents appear to disagree — UC-14 alt 2a says an unverifiable license refuses the study, while FR-13.5, SDD §4.7's schema comment and TEST-STRATEGY's S-STOA clause all say `unverified` allows study and refuses pattern intake. Read carefully they do not: FR-13.5 is the normative requirement and conditions only *intake* on a verified license, FR-13.2 (which governs studies) says nothing about licenses at all, and "unverifiable" — the Architect could not establish the terms — is a stronger state than "not yet checked", one the watchlist has no separate value for and which the Architect expresses by not registering the source (or retiring it). So `checkStudiable` reads only the pin and `checkIntake` only the license, and the researcher's own briefing says which it got.
- 2026-08-28 · (M5b.2) A research brief files as `kind: "research-brief"`, not `brief` · the standup narration took `brief` in M5.4, years of code before the Stoa existed. Overloading it would have made two unrelated artifacts answer for each other at one address, and the failure mode — a narration validated as research, or the reverse — is silent. The endpoint now dispatches six filings; the extra word is cheap.
- 2026-08-28 · (M5b.2) Research briefs file at the SAME `agent.odeon` endpoint as everything else · the M5.7 reasoning for gym proposals, applied again: ADR-0015 rejects a second governance machine, and a second filing address is a second place for filing to fail.
- 2026-08-28 · (M5b.2) `Stoa.plan()` returns a **plan, not a spawn** · the caller executes it. That is what makes NFR-17's posture assertable without running anything: S-STOA reads `envGrants` and `readOnly` off a value instead of trusting a comment above a spawn call. `envGrants` is typed `readonly []` and no parameter can fill it — a researcher reads arbitrary third-party text, and handing it a credential is the one mistake that turns a bad README into a breach.
- 2026-08-28 · (M5b.2) The checkout path is re-checked against the Agora and `worktrees/` **inside the driver**, even though the driver supplies the root it was built from · ADR-0004 gives the Agora exactly one working copy and `worktrees/` holds the company's OWN targets; a defence that depends on nobody ever passing the wrong root is not a defence.
- 2026-08-28 · (M5b.2) The injection rule lives in `prompts/stoa/study.md`, and a plan with no prompt store carries `instructions: null` · invariant §8, and NFR-17 is the instruction most likely to need rewording as real sources teach us how an attack is phrased — a rule the Architect cannot edit without a rebuild ages badly. Null rather than a silent default, because an un-briefed researcher is exactly the degradation invariant §7 forbids being invisible.
- 2026-08-28 · (M5b.2) The brief schema has a place to REPORT a directive — `directive: true` on a finding · without a field for it, the only way a researcher could mention "the README told me to do X" would be to do X. Reporting is the correct outcome (UC-14 alt 3a), the archive marks it loudly, and the `brief-archived` log event carries `directivesReported` so an incident audit can find it without opening the brief.
- 2026-08-28 · (M5b.2) Commit comparison is **prefix matching in the longer direction** · a human types a short sha onto the watchlist and a checkout reports the full one. Requiring equality would reject correct briefs over a formatting difference, which teaches researchers to distrust the validator rather than the claim.
- 2026-08-28 · (M5b.2) A brief archive file is **written once and never revised** · a second filing under an existing name is refused rather than merged (FR-13.4, invariant §5): the proposals citing a brief must keep resolving to the words their author actually read. A corrected study is a new brief, not an edit.

- 2026-08-28 · (M5b.3, latent defect found while building on it) `Gymnasium.append()` **deleted everything below its table** · it rebuilt the file as `preamble + table` and dropped the rest, so any section written under the proposals table — a human's note, or FR-14.5's mode-change record — would vanish at the next `propose()`. Nothing had ever written below the table before, so nothing had noticed. This is the M5 close-out's finding-1 class one row further down: a rewrite that silently loses a part of the document it rewrites. `append()` now preserves the postamble, and three regressions assert a mode section survives a later proposal, that a second change appends under the same heading, and that the mode table's pipe rows are never mistaken for proposal rows.
- 2026-08-28 · (M5b.3) A mode change is a **section under the ledger table, not a row in it** · UC-15's postcondition puts every mode change "in the ledger and log", but the proposals table has eight columns meant for a proposal, and forcing a mode change into them would corrupt what both the parser and the reader rely on. The Gymnasium writes the section because the Gymnasium owns that file — one writer, no second opinion about what the ledger says.
- 2026-08-28 · (M5b.3) `everEnabledImproving` is **persisted, not inferred** · the proof gate is a first-enable check (FR-14.3). Inferring "has it ever been on?" from the ledger would make a rung-3 auto-revert (FR-14.5) look like the gate had never been met and silently re-impose it — turning a safety stop into a demotion nobody asked for. The Architect can restore `improving` after a breaker revert without re-proving anything.
- 2026-08-28 · (M5b.3) A **regressed** row counts toward §6.9's "full loop" · the loop is proposed → verdict → landed → measured, and a change that was measured and missed completed that loop. ADR-0015 R2 keeps regressed rows precisely because they are evidence the company can measure its own failures; a gate that only counted successes would reward never trying anything risky.
- 2026-08-28 · (M5b.3) A gating violation refuses the gate **permanently**, and says so · every other clause is satisfiable by waiting. "A proposal reached `landed` with no Architect approval on the log" is not immaturity, it is a broken loop, and no amount of further evidence should open the gate on top of it. The refusal names the row so it can be argued with.
- 2026-08-28 · (M5b.3) The mode reaches the scheduler as a `Trigger.enabled()` predicate, checked **before the interval and without stamping the clock** · autonomy is switched off at the one place that starts autonomous work, rather than by each cadence policing itself — a job that polices itself is a job that can forget to. Not stamping the clock means a cadence forbidden for a week fires when it is allowed again instead of sitting out one more interval for having been asked while off.
- 2026-08-28 · (M5b.3) A rung-3 revert is attributed to `breaker`, never to the Architect · the log and the ledger both say who did it, so nobody reading the record later mistakes a safety stop for the Architect changing their mind. Attribution of "gym/stoa work" is by ROLE from the roster: a rung-3 stop on ordinary mission work must not switch off self-improvement for reasons that had nothing to do with it.
- 2026-08-28 · (M5b.3) The status strip shows **nothing** rather than `directed` when the mode cannot be read · FR-14.1 requires the mode visible at all times, and a mode nobody could read is not a mode that is off. Claiming the safe state on a failed read is a degradation failing as good news — the one direction invariant §7 does not allow.

- 2026-08-28 · (M5b.4) A proposal citing `RB-NNN` is **refused unless that brief is archived** (FR-13.4) · a citation to a brief nobody filed is the uncited-finding problem wearing a different hat: it looks like provenance and resolves to nothing. The link between a brief and the proposals it seeded is therefore the citation itself — no second index to fall out of date, because the evidence ref was already required to exist. The `proposed` log event carries the cited ids so the proof gate can count Stoa-seeded proposals from the log alone.
- 2026-08-28 · (M5b.4) Brief ids are matched **inside** an evidence ref, not as the whole of it · `"RB-014 finding 2"` counts. An author naming which finding they built on is being more precise, and a matcher that demanded a bare id would punish them for it.
- 2026-08-28 · (M5b.4) The standup's Stoa line is **omitted, not zeroed**, when there is no research department · "the Stoa did nothing this week" and "there is no Stoa" are different facts, and the M5.6 org-metrics decision already settled which way that goes. The mode line rides the same section (FR-14.1), so "did we do this because we were asked?" is answerable from the brief alone.
- 2026-08-28 · (M5b.4) S-MODE **archives a real brief** and cites it, rather than citing a fixture id · the §6.9 clause is "seeded by a Stoa brief", and once FR-13.4 refuses unarchived citations, a scenario that cited a literal id would have been testing nothing. Seeding the shared fixture with a brief was rejected: it perturbed S-STOA's archive counts, and a fixture that makes one suite easier by changing another's world is how fixtures start lying.
- 2026-08-28 · (M5b.4) E-STOA is a **scorer plus a discrimination test**, not an assertion about a researcher · evals are quality, not correctness (TEST-STRATEGY §6) and non-gating. `test/evals/e-stoa.ts` scores one brief against the fixture's planted patterns, noise and injection; `e-stoa.test.ts` asserts the RUBRIC discriminates — a good brief passes and each named failure mode actually fails it. A rubric nobody has tested against a deliberately bad answer will quietly pass everything on the day it matters.
- 2026-08-28 · (M5b.4) The eval's deterministic half runs in CI; the **LLM-judged half is recorded as owed** · citation presence, injection reporting and noise-only findings are computable. "Is this applicability mapping honest?" and "does the prose actually match the file it cites?" need a rubric-scoring model over a real engine, which this machine's sessions have not run. Recorded in PROGRESS with the other owed real-engine evidence rather than faked — the project's standing rule.
- 2026-08-28 · (M5b.4) Reporting the planted injection **does not cost precision** · the directive finding is a real finding. A rubric that penalised it would teach researchers to stay quiet about injections, which is the opposite of NFR-17's intent. Silently ignoring one fails the run instead — not as a security failure but as an evidence failure: it throws away the most useful thing the study found.

- 2026-08-28 · (M5b.5) The `*.tiles.json` maps **are committed**; the sheets are not · `.gitignore` now excepts them. A layout is our own work — a table of integers describing where things sit on somebody else's sheet — and it contains no asset data. Committing it is what makes ATTRIBUTION's restore path reproducible instead of a story: a fresh clone copies two PNGs into the drop and the floor paints. The sheets themselves stay out, because the LimeZu licence forbids redistributing the asset.
- 2026-08-28 · (M5b.5) Frame indices were chosen by **measuring the sheet, not by eye** · a first pass picked border tiles that looked plain at 3× zoom and would have tiled with visible seams. The second pass scored every candidate tile by per-channel colour variance and required full opacity, which found the flat fills objectively; each chosen index was then rendered back FROM its index (`x = i % columns`, `y = i / columns`) and tiled 3×3 to prove the arithmetic and the seam. `src/shared/tileset.ts` says hard-coding indices for a pack you cannot see would be fiction — measuring is how you avoid writing fiction that compiles.
- 2026-08-28 · (M5b.5) `floor-a`/`floor-b` are **near-neighbours in hue**, not just in luminance · the first mapping paired cream (240,240,200) with mint (189,241,229): almost identical luminance, opposite hue, and the floor read as a chessboard rather than one surface. Re-mapped to cream + marble-rose (244,217,212), with the indigo block reserved for `temple` where a distinct, cooler tone is the point. UI-DESIGN §1's ≤8-colours bar is about what the screen READS as, and two hues alternating per tile is two hues however close their luminance.
- 2026-08-28 · (M5b.5) A pack declares its **own required credit**, as `credit` in its tile map · the LimeZu licence requires attribution to limezu.itch.io, and the floor's status strip now prints it beside the pack name. It ships with the pack for the same reason the layout does: it is a term of *that* licence, and a credit hard-coded in the app would be wrong the moment somebody swapped the pack. Optional, because a CC0 pack owes none.
- 2026-08-28 · (M5b.5) Only the two **Room Builder** sheets were kept; the furniture sheets were extracted and then deleted · nothing maps a frame to them, and `import.meta.glob` would have bundled 2 MB of unused PNG into the shipped renderer. `seat` and `station` therefore stay procedural over a licensed floor — the "any subset" behaviour the tile map was designed for, visible in `docs/demo/m5b-floor-limezu.png`. Compositing a custom atlas from both sheets is permitted by the licence ("edit and use") but is a bigger decision than this package, and is left to the Architect.
- 2026-08-28 · (M5b.5) The Kenney CC0 packs were **retired for quality, not licence** · their terms would still permit use. §7's bar is the LimeZu line, and keeping two half-mapped packs in one drop invites exactly the bug `resolveTileset` guards against — painting one pack's frames out of the other's sheet.
- 2026-08-28 · (M5b.5) The character generator shipped in the purchase is **never run**, and its character sheets are never used · ATTRIBUTION rule 3 exists so no likeness and no other IP's character can appear on the floor, and a purchased character pack is precisely the temptation it was written against. Citizens stay procedural.
- 2026-08-28 · (M5b.5) `test/renderer/tileset.test.ts` gained a block that validates **the actual installed drop** when there is one · the sheets are gitignored, so CI runs it with nothing to check; on a machine where the packs are restored it is the only thing that catches a map naming a missing sheet, an index past the end of the sheet, or a wrong `columns`. PNG dimensions are read straight from the IHDR chunk rather than adding an image decoder for one arithmetic check (invariant §10).

- 2026-08-28 · (M5b.6, DEFECT FOUND BY THE EXIT DEMO) The proof gate read **every seeded ledger row as a gating violation**, which made the gate unopenable on any real company · `checkProofGate` treated "reached `landed` with no `approved` event on the log" as the absorbing failure. But a ledger seeded from the build-phase archive (FR-12.6) inherits rows the Architect genuinely DID decide — the archive records the Decided date — while the fresh log has no `gym` events for them at all. So GYM-001…005 each produced a violation, and because a violation can never be waited out, `improving` could never be enabled by any company that inherited an archive. Which is every company. Every unit test passed because each one synthesised the log it wanted; the seam only showed when the running app read the REAL seeded ledger beside a REAL fresh log — the same seam-blindness class the M3, M4 and M5 audits each found a different way. A verdict now counts from EITHER input, which is what FR-14.3 permits ("the Gymnasium ledger and event log"), and the violation is reserved for a row with no verdict in either place. Regressions cover the seeded shape and the genuinely-undecided one.
- 2026-08-28 · (M5b.6) The reading desk offers a **pin field** · M5b.1 deferred pin-setting to M5b.2's study flow and M5b.2 built `plan()`, which only READS the pin. Neither package owned setting it, so every source registered from the desk was permanently unstudiable — a hole the exit demo walked straight into. The register draft already carried `pin: string | null`; the panel was hard-coding `null`. Empty still means unpinned, which is a legal and visibly-refused state (FR-13.2).
- 2026-08-28 · (M5b.6) The exit demo drives the **real desk and the real router** · the URL is registered by filling the actual inputs and clicking the actual button (real IPC, no harness write), and the brief and proposal arrive as files in a real agent outbox so Hermes, the Odeon endpoint, the Stoa archive and the Gymnasium all run their shipped paths. Only the researcher's ENGINE is stood in for. The router earned its keep on the way: it refused the first three attempts for a malformed id, a missing `needs_human`, and `requires_reply: false` on a `propose` (ADR-0003's obligation table) — each parked in `outbox/.rejected/` with the reason, exactly as designed.
- 2026-08-28 · (M5b.6, DEFECT FOUND BY CI) The drop-validation block guarded **every** check on "a map exists", which broke the moment the maps became tracked · committing the `*.tiles.json` maps (M5b.5, deliberately) meant CI suddenly had maps and — correctly — no sheets, so "names a sheet that is installed" and "frames all land inside the sheet" failed on ubuntu while passing on the machine that has the drop. Two populations, checkable in two places: the MAPS are our own work and CI should validate their shape; the SHEETS are the licensed asset and anything that opens one runs only where the drop is restored. Verified both ways before pushing — 33 cases with the sheets moved aside, 35 with them present.

- 2026-08-29 · (M5b close-out audit, evidence integrity) The exit demo's research cycle was **source-fake beyond its disclosed stand-in** · pin `9f3c1de` exists in neither `sst/opencode-sdk-js` nor `sst/opencode`, the cited path is another repo's layout, and the GYM-006↔RB-002 pairing survived in no artifact — the harness's provenance check compares brief-commit to watchlist-pin and cannot see a remote, so a typed pin that never existed passed every internal gate. The exit-review row is amended in place, and the cycle re-run at close against munder-difflin @ `b91a49f` (verified via `gh api` AND a local clone inside the run) through the shipped path — `docs/demo/m5b-cycle-real-source.txt`. Standing lesson for the demo discipline: an exit demo's external references get the same verification its internal ones do.
- 2026-08-29 · (M5b close-out audit, fixed) `Stoa.brief(id)` seeds before reading, as `briefs()` always did · the first-call order decided whether a seeded brief existed; a fresh home refused a proposal citing `RB-001`. First-call regression test added; the half-seeded-seam class is now three-for-three across audits (gym proposals, gym parser, this) — seed-on-EVERY-read-path is the rule.
- 2026-08-29 · (M5b close-out audit, fixed) The Stoa cadence body is the shipped `stoa-cadence.ts` · S-MODE and the scheduler suite each rebuilt the trigger inline, so the production body was exercised by nothing — the exact copy-of-the-wiring class the rig's own comment records. `index.ts` and both suites now run one tick, which returns what it logged so the tests assert the record, not a copy.
- 2026-08-29 · (M5b close-out audit, fixed) `isImprovementRole` is exact and shared, not a substring in `index.ts` · `includes('improv')` would have reverted the company mode over a rung-3 stop on a mission hire named "process-improver-docs". Roles come from ADR-0019's vocabulary (`researcher`, `improver`), extended visibly in `shared/mode.ts`, table-tested with the audit's counter-example.
- 2026-08-29 · (M5b close-out audit, recorded not fixed) Deferred with reasons · FR-14.1's mode tag rides `cadence-fired` alone until M7 creates more autonomous record types; the §6.9 `stoaSeeded` count reads `gym`/`proposed` log events only, so a company inheriting a measured build-phase archive under-counts — the STRICT direction (the gate waits, never opens early), fix rides the M7 gate work; Stoa spend attribution joins the gym-spend deferral (M6.7); no path advances a watchlist pin (`stoa:pin` channel or study-recorded pin — the owed list carries both); `prompts/stoa/rank.md` and `study-refused.md` await the Artemis-ranking leg; `plan.question`'s connective sentence is invariant-§8-adjacent (serialization otherwise); the reading-desk pin fix carries no renderer regression until M6.1 establishes a DOM harness.

- 2026-08-29 · (M6.1) The renderer test harness is **`react-dom/server` static markup**, not a DOM library · the M5b close-out owed the reading desk a renderer regression "once M6.1 establishes a DOM harness", and jsdom/happy-dom would be a new dependency — a BUILD-PROMPT §8 must-ask. Static markup renders the SHIPPED component bodies (never a copy of them, the M5b rig lesson) with nothing added to the tree: `esbuild: { jsx: 'automatic' }` in the vitest config plus a fourth tsconfig project, `tsconfig.web-test.json`, because the node project has no `jsx` setting and no DOM lib and widening it would loosen the whole main/shims/test surface for three files. What it does NOT cover — effects, clicks, state transitions — is tested at the module boundary instead (BUILD-PROMPT §6): the pin mapping moved out of the component into the exported pure `registerDraft`. A jsdom upgrade for interaction coverage is carried as a must-ask in the session report, not decided here.
- 2026-08-29 · (M6.1) `SourceRow` and `BriefCard` are exported presentational components · the reading desk's data-dependent branches (`pin: —` for an unpinned source, the struck-through retired row, the two degradation notes) only exist when rows are present, and effects do not run under static markup. Extracting them makes the shipped row body reachable without a test-only prop on the panel. Behaviour unchanged.
- 2026-08-29 · (M6.1) Role → silhouette is an **exact-match table with a deterministic hash fallback** · §5.1's six roles are named by category ("Scribe / docs", "Builder / code"), so the table keys are those words plus the two role strings the company already uses (`orchestrator`, `engineer`). Exact equality, never a substring test: a hire named "process-improver-docs" matching on "docs" is the M5b audit's `includes('improv')` defect one domain over. Unknown roles hash to one of the five assignable silhouettes — stable and total, rather than a guess dressed as a match. `orchestrator` is unreachable by hash: it is Artemis's alone.
- 2026-08-29 · (M6.1) The `working` overlay is declared as kind `token` with **no pixels until M6.3** · §5.2's working row IS §5.3's carrying token, and §5.3 is M6.3's package. Drawing a placeholder mark would be a shape projecting no fact — the "invented motion" §5.4 forbids one layer up. The phase still reads: `shared/badges.ts` names it in shape and in the floor census. The overlay test asserts the empty-until-§5.3 seam explicitly so it cannot be forgotten.
- 2026-08-29 · (M6.1, DEFECT FOUND BY THE LIVE RENDER) Citizens gained a **1 px ink silhouette backing** · the first render of the §5.1 sprite passed all twenty unit tests — 8 drawn directions, frame counts, bob at frame boundaries, head-room clear, ≤5 colours, prop sizes — and looked like a stack of boxes that dissolved into the floor, because `skin` is `sand` (`#c9a05c`) and that is exactly `worldTerraceA`. Asserting the spec is not the same as meeting the §7 quality bar. Fixed generally rather than by re-picking one colour: every body part emits an ink backing one pixel larger on each side, all backings drawn before all fills so the outline is continuous. This is why the head starts at row 10 — `10 − 1 (bob) − 1 (backing) = 8`, the first row §5.1 allows a body pixel. Anatomy redrawn at the same time: hairline, neck, tunic hem, sleeve-plus-hand arms.
- 2026-08-29 · (M6.1) `hairSide` gives each view an **asymmetric skull mass** · with symmetric sideburns the `e`/`w` profiles read as slightly narrow front views. The hair mass sits on the side turned away from the viewer, which is the strongest directional cue available at 32 px — and a second reason a runtime flip would be wrong, since a flip would carry the back of the head round to the front.
- 2026-08-29 · (M6.1) `docs/demo/m6-1-citizens-v2.svg` is generated **from the shipped modules** · the contact sheet (8 directions × 4 frames × 6 silhouettes, plus every §5.2 overlay frame) is rendered by importing `citizenSprite` and `overlayPixels` themselves, so the evidence cannot drift from the code. SVG rather than a screenshot because it is text, diffable, and needs no image encoder (invariant §10). The generator is a scratch file, not committed — the artifact is.

- 2026-08-29 · (M6.2) The §5.4 size catalog lives in `shared/floor.ts`, the state model in `shared/stations.ts` · geometry is what the plan needs and the state model does not, so the dependency runs one way and there is no cycle between them. `floorPlan()` now claims each station's whole FOOTPRINT (`stationFootprint`) instead of one tile — before M6.2 the Odeon was 96×64 in the document and 32×32 on the floor, which made §5.4's size column decorative. The Odeon's anchor moved from col 17 to col 16 so its third column is not the east wall.
- 2026-08-29 · (M6.2) `stationView()` returns a `because` string, and it is not decoration · §5.4 ends "every state maps to an event-plane fact; no station animates on a timer alone". Making the reason part of the return type turns that from a rule someone must remember into one the compiler asks about: a station somebody wants to animate for looks has nothing to put in the field. It doubles as the §8 information-parity text — `stationCensus()` is the same facts in words, so nothing on the floor is visible only as motion (NFR-15).
- 2026-08-29 · (M6.2) `AvatarUpdate` gains `pendingMail`; `AvatarSnapshot` does NOT · the desk tray flag IS `pendingMailCount` (§5.4), and it must arrive with the same agent's snapshot at the same moment or the floor can raise a flag on a citizen who has just consumed the mail. It rides the addressed wrapper rather than the SDD §6 state machine, because mail is not one of that machine's states. One source in main (`pendingMailFor`) feeds the autonomy loop, the push AND the `avatars:list` handler — the M5b lesson is that a fact on the listing path and not the others is a seam no unit test sees, so a regression asserts the listing path carries it.
- 2026-08-29 · (M6.2) Compositions and furnishings ride the pack's `*.tiles.json`, never code · §5.4 stations are multi-tile structures and §5.7 furnishings are place identity; which frames compose either is a property of the PACK, so both are optional map fields validated like the rest. `validateCompositions` enforces `frames.length === cols × rows` AND that the footprint matches the §5.4 catalog — a silently mis-sized composition is a station painting over its neighbour. A broken entry degrades that station to the single frame and then to the procedural painter (invariant §7); it never throws. Both checks are map-only, so CI runs them with no sheets present (the M5b drop-guard rule).
- 2026-08-29 · (M6.2, DEFECT FOUND BY THE LIVE FLOOR) `PlanCell` gained `part` · with footprints landed, the painter still painted per tile, so a 64×32 desk came out as two 32px desks and a row of them read as one long slab — and a 2×2 station showed four gold markers. The plan now tells each cell where it sits inside its structure and how big that structure is, so the inset is applied only on outer edges and the marker once per structure, on its anchor. Second time this milestone the running floor said something a green suite could not: M6.1's citizens dissolved into the terrace, and these were wallpaper.
- 2026-08-29 · (M6.2) The floor polls `odeon.meeting()` at 5 s · the Watch brazier reads `watch.approvals()` and the existing `gate:open` nudge, but SDD §5's event list has no push channel for a live meeting, so the Odeon fill is polled at the cadence the panels already use. This is the same gap the `odeon:queue` status-strip badge carried item names, and it closes on M6.7's scheduler work.

- 2026-08-29 · (M6.3) Every envelope is built from a LOG ENTRY, never from a renderer event · `envelopeFor(entry)` takes its identity from `msgId`, its colour from `act` and its start from the entry's own `ts`, so replaying `log.jsonl` flies the same envelopes at the same moments. That is NFR-13's spirit in the vfx layer: a renderer that minted ids or read its own clock would hold state the record cannot account for, which is the second source of truth ADR-0014 forbids the floor. The floor follows `log:append` and re-reads the tail, seeding its cursor from the newest entry so a window opened mid-run does not replay the day's mail as a storm.
- 2026-08-29 · (M6.3) §5.3's **search** row has no tool class, and §6's **meeting** class has no token · SDD §6's station map is the normative list of classes (file/shell/web/mcp/ledger/meeting) and has no `search`; UI-DESIGN §5.3 prints a `search` row anyway. The row is left unreachable rather than inventing a class — adding one is an SDD change, not a floor change. Conversely `meeting` gets `null`: a citizen comes back from the Odeon carrying nothing, which is honest, where invented art would not be. The table is total over `ToolClass` so the compiler keeps it that way.
- 2026-08-29 · (M6.3) The token comes from the STATION a citizen is leaving · §5.3 keys tokens by tool class, but an avatar snapshot carries only its station. `STATION_FOR_TOOL_CLASS` is injective, so `toolClassForStation` recovers the class without adding a field to the SDD §6 snapshot and without the floor ever learning a tool NAME (NFR-12). A regression asserts no Claude-ism (`Read`, `Bash`, `WebFetch`, …) resolves to a token.
- 2026-08-29 · (M6.3) Reduced motion is read from `prefers-reduced-motion`, not a new app setting · it is the signal the platform already gives, it needs no IPC or config schema, and it is live (a reader may change it mid-session). §8's parity is asserted as EQUALITY between the moving form's information and the still form's — `reduceEnvelope(f).info` must equal `envelopeInfo(f)` — rather than as the presence of a label, because "there is a label" is exactly the check that passes while the label says nothing useful.
- 2026-08-29 · (M6.3) `test/main/vfx-seam.test.ts` delivers REAL mail through the REAL router and asks the model what flies · the unit suite writes its own log entries, which is the two-correct-halves blindness every audit here has found. Demonstrated rather than asserted: renaming Hermes's `msgId` field leaves `test/shared/vfx.test.ts` 24/24 green and fails three seam cases. Bounce, hop-cap divert and delivery are each driven through the shipped path.

- 2026-08-29 · (M6.4) `seam.ts` is ADR-0007's three interfaces and nothing more · `SpeechToText`, `TextToSpeech`, optional `DuplexVoice` — transcribed, not extended. The M1.1 lesson: an adapter interface that grows a convenience method becomes a contract every future adapter owes, and the ADR is where that decision belongs. There is deliberately no `shouldFailover()`, `retry()` or provider preference anywhere on the seam: an adapter's whole obligation on failure is to classify the fault and hand it up, and the conformance suite asserts no such method exists.
- 2026-08-29 · (M6.4) Three fault classes, because the policy treats exactly three differently · `auth` (retrying cannot help — the provider is done for the session), `transient` (worth another provider and a later manual failback), `latency-breach` (answered, but too slowly to be a voice). TEST-STRATEGY §5 asks for "auth vs transient vs latency-breach → correct failover state machine transitions", so the taxonomy is the seam's, and each adapter maps its provider's own shapes onto it — recorded in the fixtures, since ElevenLabs errors are HTTP-shaped and Realtime's arrive as session events.
- 2026-08-29 · (M6.4) The failover machine is ONE-WAY; failback is manual · ADR-0007 says so in two words, and the reason is operational: a provider that failed auth will fail it again, and one that breached latency once will do it under the same load, so a timer-based recovery would flap the session. A fault burns its provider for the session; `cooldown` (no provider, voice off, everything else in text per FR-8.6) is the honest end state. A stale fault from a provider already left is ignored — acting on it would burn the provider just switched TO.
- 2026-08-29 · (M6.4) The repeat-back TOKEN is derived from the gate; the SENTENCE is config · FR-8.4 needs the Architect to say something specific, so `repeatBackToken()` builds `confirm <first three words of the action>` — "confirm" alone would be a bare assent with extra steps. The asking sentence lives in `prompts/herald/phrasebook.md` (invariant §8), and the policy emits a phrase-book KEY, never a sentence; a test greps the policy source for spoken prose. Bare assents are refused with `because: 'bare-assent'` rather than a generic mismatch, so the Herald can explain itself instead of pretending it did not hear.
- 2026-08-29 · (M6.4) The voice conformance suite runs on RECORDED FIXTURES and ships before the adapters · a suite needing a key would not run in CI, and an adapter that only proves itself when someone is watching proves nothing. `test/fakes/fake-voice.ts` replays `test/fixtures/voice/*.json` — real provider error shapes and measured latencies, no key, no network, no audio, and a test that asserts no secret-shaped string got into a recording. M6.5/M6.6's adapters join the same list and nothing about the suite changes (NFR-12).

- 2026-08-29 · (M6.5, ARCHITECT-APPROVED DEPENDENCY) `@elevenlabs/elevenlabs-js@^2.65` and `openai@^7.8` added · a BUILD-PROMPT §8.3 must-ask, put to the Architect with three options and approved on the spot. The alternative was hand-rolling two streaming wire protocols (ElevenLabs realtime STT, OpenAI Realtime duplex) with no key in this environment to verify either — code that would be asserted only against fixtures I wrote myself, which is precisely the plausible-but-unverifiable class the M5b audit caught. The eslint boundary confining voice SDKs to `src/main/herald/` already existed and anticipated exactly this. `npm audit` reports 2 high-severity findings; both are pre-existing (electron, pinned `^37` by the M0 working constraint, and `extract-zip` beneath it), neither introduced by the SDKs.
- 2026-08-29 · (M6.5, MY OWN M6.4 SEAM WAS INCOMPLETE) `SpeakOptions.onAudio` added · ADR-0007 says "streamed audio with cancel", and the M6.4 transcription had the cancel with no way for the audio to leave. The first real adapter found it — which is the argument for writing an adapter against a seam rather than admiring the seam. Completing the ADR's own sentence, not extending it.
- 2026-08-29 · (M6.5) `spokenSoFar()` is a MEASUREMENT · the SDK's `streamWithTimestamps` returns character alignment, so the "unspoken from here" mark VOICE-DESIGN §2 requires is the characters the provider actually produced audio for — not an estimate at 150 wpm. When the alignment drifts from the text, `bargeIn` treats a non-prefix as "nothing was heard", which is the safe direction: claiming the Architect heard something they did not is the worse error.
- 2026-08-29 · (M6.5) The key arrives through an injected getter, never `process.env` · `check-invariants.cjs` PERMITS `process.env` under `herald/`, so the permission needed a second check: a test strips comments from the adapter and asserts the code does not read the environment. Voice id and both model ids load from `prompts/herald/*.md` and a test asserts neither appears in the adapter source — a voice id in code is the persona in code by another name (FR-8.5, invariant §8).
- 2026-08-29 · (M6.5, TRIPWIRE FALSE POSITIVE) The voice-SDK lint patterns gained `!./**`, `!../**` · eslint's `no-restricted-imports` `group` uses gitignore semantics, so the bare pattern `elevenlabs` matched `../../src/main/herald/elevenlabs` — our OWN adapter, which SDD §8 names `elevenlabs.ts`. Rather than rename the file away from the SDD or exempt the test directories (which would let a test import the real SDK), the patterns now exclude relative specifiers: a relative import can never be an SDK import. Verified BOTH directions — the rule still errors on `import from 'elevenlabs'` outside `herald/`, and no longer fires on the shipped adapter.

- 2026-08-29 · (M6.6) `DuplexSession.say(text)` added, for the same reason `onAudio` was · ADR-0007 gives OpenAI Realtime one job — be the TTS fallback — and says "the policy layer maps the common conversation contract onto it". A duplex session that could only carry captured audio could not do that job at all. Both seam additions come from sentences already in the ADR; neither is a convenience the interface grew on its own, and the pattern is worth naming: transcribing an ADR's interface NAMES is not the same as transcribing its contract, and only writing an adapter finds the difference.
- 2026-08-29 · (M6.6) The Realtime adapter implements `DuplexVoice` and ONLY that · it does not synthesise a `TextToSpeech` out of the duplex session to look like a complete adapter. ADR-0007 assigns the mapping to the policy layer, and an adapter that faked the other two interfaces would make the policy choose it for capabilities it does not have. `HeraldSession.speakWith` holds the mapping: a `tts` adapter is spoken through, a duplex-only adapter is asked to `say`.
- 2026-08-29 · (M6.6) Production supplies the Realtime transport; every test injects it · the SDK's `OpenAIRealtimeWebSocket` needs the runtime's native `WebSocket`, present in Electron's Node and absent under vitest on Node 20.16. With no `connect` the adapter reports `{ok: false, fault: 'transient'}` and refuses to open — an honest, visible degradation the policy can act on (invariant §7) rather than a crash inside the SDK.
- 2026-08-29 · (M6.6, DEFECT THE FIRST GREEN SUITE HID) A mid-utterance failure was losing what the provider HAD said · `await handle.done()` rejects, so `spokenSoFar()` was never read and the fallback re-spoke the whole line. Every S-FAILOVER case passed anyway, because "the whole line reached the transcript" is true either way — the suite was asserting the wrong half of "continuous". `speakWith` now credits the partial on the failure path, and the continuity case asserts what the FALLBACK was asked to say (the remainder, not the line). Mutation-checked: dropping the partial credit fails it.

- 2026-08-29 · (M6.7) The Herald parses the ARCHIVE, not the filing · `narrationOf(markdown)` reads the sentences back out of the artifact `renderBriefMarkdown` wrote, so there is no path by which a sentence absent from the archive could be spoken. Taking a `BriefFiling` instead would have been easier and would have let the spoken brief drift from the written one with nobody noticing until an audit read both. The archive is also the artifact that PASSED `checkNarrative` (SDD §7.2), so speaking anything else would be speaking prose nothing had checked — the E-BRIEF-FAITH failure. The `## Source refs` appendix is excluded: it is the audit trail behind the narration, not part of it.
- 2026-08-29 · (M6.7) A voice approval needs repeat-back for spend even when the GATE did not ask · FR-8.4 names destructive AND spend; `gate.requiresRepeatBack` is set by gate policy, which is a different question from "may a voice approve this". `voiceApprovalAsk` requires a token when EITHER says so, so a policy that had not marked a spend gate cannot let one through on a bare "yes". A refusal returns the line to say and the reason and leaves the gate open — "I could not confirm you meant it" and "no" are different answers, which is the distinction `GateManager` already draws.
- 2026-08-29 · (M6.7) The metric-check tick RAISES; it does not measure · SDD §7.6 puts the scheduler in the cycle ("row `landed` ─► scheduler books metric check") and nothing booked one, so a landed change whose check nobody remembered to run quietly counted as a success — the gamed-metric failure ADR-0015 opens by warning about. `gymCadenceTick` logs every due check with its declared metric and raises it; the measured VALUE still arrives through `measure()`, because booking a check and deciding what the number was are different jobs. A check stays due until somebody runs it (the row stays `landed`), which is deliberate: a booking that fired once and forgot would let a missed check disappear, and R2 says the ledger is total.
- 2026-08-29 · (M6.7) Spend attribution takes a `TokensFor` lookup, not a row list · production passes the `CostLedger`'s own `spendFor(...).cumulativeTotals` (durable, so a restart cannot zero it — invariant §11) and tests pass a table, so both reach the same number by the same route. Two paths to one figure is exactly how a total drifts from the ledger it claims to come from. Attribution is by EXACT role (`isImprovementRole`), so a hire named "process-improver-docs" is not counted; and `slice()` now returns `source` beside the number, because the brief is read ALOUD, where there is no card to hover and a bare total invites trust in a scope the listener cannot see.
- 2026-08-29 · (M6.7) `CountBadge` moved to its own module · the `odeon:queue` badge and the gate badge obey one rule ("an unknown count must never render as reassurance"), so they share a component; it lives outside `App.tsx` because importing `App` pulls in xterm and Pixi, which need a browser, and chrome carrying a rule worth pinning should be reachable from the M6.1 render harness without booting the shell.
- 2026-08-29 · (M6.7, OWED NOT FAKED) The wake word is a SETTING, not a detector · `config.wakeWordEnabled` advertises the mode (FR-8.3) and `activeModes` already handles it, but no local wake-word engine is an approved dependency and shipping one that only pretended to listen would be worse than the gap. Push-to-talk is unaffected — it is always available, so enabling the flag adds a way in and never removes one. Detection is owed to a session that can take the dependency question.

- 2026-08-29 · (M6.8, TWO DEFECTS FOUND BY RUNNING THE BOOKING ON THE REAL LEDGER) The metric sweep is booked, and nearly was not · `metricChecksDue` matched `^due YYYY-MM-DD$` anchored, and GYM-003's real cell reads `due 2026-09-11 (live-quit evidence owed with the metric check)` — so the ONE row the M6 window singles out was the one row that would never have been raised. Rows a test writes for itself are tidy; a ledger a human maintains is not, which is why the exit review ran the shipped booking against `docs/gymnasium/LEDGER.md` instead of against fixtures. Match relaxed to a `due <date>` prefix with the note kept; regression added quoting the real cell. Second defect, same run: GYM-001's due date (2026-09-25) lived only in its metric prose while its Measured cell was `—`, so it was invisible to the scheduler; the ledger now records it where the booking reads. Re-verified after both: 2026-09-11 raises GYM-002/003/004, 2026-09-25 adds GYM-001.
- 2026-08-29 · (M6.8) Committed floor evidence paints PROCEDURALLY · `docs/demo/m6-floor-v2.svg` renders from the shipped painter, citizen, station-art and overlay modules with no pack installed, because the licensed sheets are not in the repository and evidence that needed them would be evidence CI could not reproduce. The live captures (real Electron app with the LimeZu pack; the shipped FloorCanvas with six citizens) are described in the exit review for what each one is — a real app, and a real renderer with a stubbed bridge — rather than blurred together.

- 2026-08-29 · (M6 CLOSE-OUT AUDIT, ARCHITECT-APPROVED SPEC AMENDMENT) The repeat-back token carries the WHOLE subject, matches EXACTLY, and is single-use · the three-agent close-out audit proved by execution that FR-8.4's safety behaviour did not hold. `repeatBackToken` kept the first three words of the gate's subject, which read as faithful to the clause's own example ("say confirm delete") and was not: `delete branch release/9` and `delete branch release/10` produced one token, and every spend gate collapsed to `confirm raise the daily` — so the AMOUNT, which is the whole subject of a spend approval, was absent from the words approving it. Worse, `checkRepeatBack` used `said.includes(wanted)`, so the spoken refusal "no, do not confirm delete branch release 9" returned `{confirmed: true}`: the refusal quotes the token, and quoting was enough. Put to the Architect as a BUILD-PROMPT §8.3 must-ask with three options; the strongest was chosen. The token now carries the normalized subject entire, the match is exact, and `repeatBackChallenge` issues a nonce with a two-minute lapse so an answer is single-use and a stale asking cannot be answered. FR-8.4 and VOICE-DESIGN §3 were amended with the change rather than after it. The cost is a trailing "please" now failing — deliberately the right side to err on for the only spoken act that cannot be undone. Mutation-checked in both directions.
- 2026-08-29 · (M6 CLOSE-OUT AUDIT) `validateCompositions` is CALLED · its own contract comment said a bad composition "degrades that station to the procedural painter and says so (invariant §7)", and the function had only test callers. The degrading half was real (`compositionFor` returns null); the saying-so half was not, so a pack shipping a wrong-sized station silently lost it. `resolveTileset` now appends the problems to the visible `note`, and the pack still installs — one wrong entry must not cost the whole pack. This is the invariant-§7 failure mode worth naming: a degradation path that works, beside a disclosure path nobody wired.
- 2026-08-29 · (M6 CLOSE-OUT AUDIT) `Gymnasium.slice()` emits `spendSource`, the name the brief READS · the producer returned `source` and `BriefInput.gymSlice` declared `spendSource`; nothing anywhere set it, so the true branch in `brief.ts` was dead and M6.7's "the brief prints both" was false. Object spread bypasses excess-property checking, which is why `typecheck` stayed green through two milestones — and both halves had passing tests, because no test spanned the seam. Renamed at the producer, and the regression now drives the REAL slice into the REAL compiler. The general lesson is the M5b one restated: two correct halves that have never met are not a feature.
- 2026-08-29 · (M6 CLOSE-OUT AUDIT) `.gitattributes` added; three files renormalized to LF · `docs/PROGRESS.md` entered the index with CRLF at `5b9ff87` and stayed that way for the rest of M6, so the milestone's diff read as 6582 changed lines where 248 had changed. Nothing was wrong with the content — I verified the only real deletions were the nine `- [ ]` boxes becoming `- [x]` — but PROGRESS.md is the document a close-out audit reads BY DIFF, and a record that cannot be reviewed by diff is where a substantive edit to an earlier milestone would hide. `core.autocrlf` is per-machine and cannot be relied on; `.gitattributes` travels with the repository.

- 2026-08-30 · (ARCHITECT DECISION) **M6.9 — wiring the Herald into the application — is DEFERRED INDEFINITELY** · the Herald is not an important function for the Architect right now, so the effort belongs elsewhere. Recorded as a deferral rather than a descope, which matters for three reasons. (1) The close-out audit's finding is untouched: 1 406 lines under `src/main/herald/` still have no production caller, and the record still says so. Nothing is being quietly reclassified as fine. (2) Nothing is wasted — the ADR-0007 seam, the policy, both adapters, the session and the narration are built, tested, conforming and audited; they are a library waiting for a caller, and M6.9 remains the fix whenever it is called for. (3) The consequence is stated instead of absorbed: all three of M6's exit criteria in `docs/IMPLEMENTATION.md` are voice-live (SRS §6.2 live, §6.5 live, a full day driven by voice), so with M6.9 deferred they are unreachable by construction and M6 cannot close on its criteria as written. That is now an OPEN ARCHITECT DECISION — amend the criteria, waive them on the record, or hold M6 open — and it blocks the M7 start under BUILD-PROMPT §5. The alternative, silently dropping the criteria so the milestone could be ticked, is exactly the failure the close-out audit was convened to catch.
