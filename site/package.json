{
  "name": "ephesus-site",
  "type": "module",
  "private": true,
  "version": "0.1.0",
  "description": "The Ephesus project website. A separate workspace: its own dependency tree, excluded from the app's install and CI.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "astro": "5.14.1",
    "sharp": "^0.35.4"
  }
}
