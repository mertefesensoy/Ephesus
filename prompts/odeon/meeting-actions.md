Meeting {{meetingId}} closed with these action items:

{{actions}}

Propose them to the ledger endpoint. You are the ledger's only scribe (FR-4.2),
so nothing here reached `tasks.json` on its own — the minutes record what was
agreed, and the ledger records what somebody now owes.
