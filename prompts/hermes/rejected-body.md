Your outbox file {{file}} was not carried. Nothing was delivered, and nobody
read it.

{{reason}}

The file is parked at {{parked}}, still exactly as you wrote it. Read it there,
fix what the reason above names, and write the corrected message into your
outbox as a new file with a new id. Re-sending the parked file unchanged will
be refused the same way.
