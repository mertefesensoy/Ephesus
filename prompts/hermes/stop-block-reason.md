You have {{pendingMail}} unread message(s) and {{pendingTasks}} unfinished task(s).

Read your inbox first: the messages are in your `inbox/` directory, one JSON file
each. Handle them in the order they arrive. When you have dealt with a message,
reply through your `outbox/` if it asked you for something — `request`, `query`
and `propose` all expect an answer, and the company is waiting on it.

Then continue any task still assigned to you. If you cannot make progress, say so
in a message rather than stopping silently: an unanswered question stalls whoever
asked it.
