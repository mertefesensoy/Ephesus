The study was not started: {{reason}}

Refused rather than skipped (UC-14 alternate 2a), so the reason is on the record
instead of a study quietly never happening. Nothing about the watchlist changed;
the Architect decides what to do next.
